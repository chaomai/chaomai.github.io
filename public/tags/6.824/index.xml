<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
    <channel>
        <title>6.824 on chaomai&#39;s-blog</title>
        <link>http://localhost:1313/tags/6.824/</link>
        <description>Recent content in 6.824 on chaomai&#39;s-blog</description>
        <generator>Hugo -- gohugo.io</generator>
        <language>en-us</language>
        <lastBuildDate>Mon, 19 Aug 2019 19:56:05 +0000</lastBuildDate><atom:link href="http://localhost:1313/tags/6.824/index.xml" rel="self" type="application/rss+xml" /><item>
        <title>6.824 2018 Lecture 6 Fault Tolerance Raft</title>
        <link>http://localhost:1313/posts/2019/2019-08-19-6.824-spring-2018-lecture-6/</link>
        <pubDate>Mon, 19 Aug 2019 19:56:05 +0000</pubDate>
        
        <guid>http://localhost:1313/posts/2019/2019-08-19-6.824-spring-2018-lecture-6/</guid>
        <description>&lt;h1 id=&#34;readings---in-search-of-an-understandable-consensus-algorithm-extended-version-section-6-to-end论文&#34;&gt;Readings - In Search of an Understandable Consensus Algorithm (Extended Version) (Section 6 to end)论文
&lt;/h1&gt;&lt;h2 id=&#34;集群成员变更&#34;&gt;集群成员变更
&lt;/h2&gt;&lt;p&gt;实际应用中，常常会有配置变更的需求，即：成员变更。手动的方式有下面两种，&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;把集群整体下线，配置修改完毕以后再上线是可行的，但会造成服务不可用。&lt;/li&gt;
&lt;li&gt;新server可以通过获取其ip来替换集群成员，需要保证被替换的server不会再加入集群。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;但这两个方式都有明显的弊端，且任何手动的步骤都有引起错误的可能。&lt;/p&gt;
&lt;p&gt;配置切换需要保证安全性，在同一个term内，集群不能够同时存在两个leader。由于无法一次性原子的切换所有server的配置，一次增减多个server并直接切换配置可能会出现disjoint majorities的情况。&lt;/p&gt;
&lt;p&gt;raft变更的方案有两种：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;single-server change&lt;/li&gt;
&lt;li&gt;使用joint consensus&lt;/li&gt;
&lt;/ol&gt;
&lt;h3 id=&#34;single-server-change&#34;&gt;single-server change
&lt;/h3&gt;&lt;p&gt;每次增减一个server。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;配置的变更&lt;/strong&gt;
具体变更过程如下，&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;leader收到变更请求，AppendEntries RPC按新配置发送$C_{new}$。&lt;/li&gt;
&lt;li&gt;每个server收到$C_{new}$后&lt;em&gt;立即生效&lt;/em&gt;。&lt;/li&gt;
&lt;li&gt;新配置下，$C_{new}$复制到大多数server，则达成committed。
&lt;ul&gt;
&lt;li&gt;此时，就算有剩下的server未得到新配置，也不会构成多数派，&lt;/li&gt;
&lt;li&gt;且，未得到新配置的server也不被选举为leader。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;$C_{new}$提交后，&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;leader可以响应client，告知本次配置变更已经完成。&lt;/li&gt;
&lt;li&gt;如果配置是移除一个server，那么这个server可以下线了。&lt;/li&gt;
&lt;li&gt;可以开始下一次配置更新。&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;strong&gt;安全性&lt;/strong&gt;
&lt;img src=&#34;http://localhost:1313/images/2019/15662026435774.jpg&#34;
	
	
	
	loading=&#34;lazy&#34;
	
		alt=&#34;-w543&#34;
	
	
&gt;&lt;/p&gt;
&lt;p&gt;总共有四种情况：&lt;/p&gt;
&lt;table&gt;
  &lt;thead&gt;
      &lt;tr&gt;
          &lt;th&gt;&lt;/th&gt;
          &lt;th&gt;member change&lt;/th&gt;
          &lt;th&gt;变更后达成disjoint majorities的条件&lt;/th&gt;
      &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
      &lt;tr&gt;
          &lt;td&gt;奇数个成员，增加一个&lt;/td&gt;
          &lt;td&gt;2k+1 -&amp;gt; 2k+2&lt;/td&gt;
          &lt;td&gt;old = k+1, new = k+2&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
          &lt;td&gt;奇数个成员，减少一个&lt;/td&gt;
          &lt;td&gt;2k+1 -&amp;gt; 2k&lt;/td&gt;
          &lt;td&gt;old = k+1, new = k+1&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
          &lt;td&gt;偶数个成员，增加一个&lt;/td&gt;
          &lt;td&gt;2k -&amp;gt; 2k+1&lt;/td&gt;
          &lt;td&gt;old = k+1, new = k+1&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
          &lt;td&gt;偶数个成员，减少一个&lt;/td&gt;
          &lt;td&gt;2k -&amp;gt; 2k-1&lt;/td&gt;
          &lt;td&gt;old = k+1, new = k&lt;/td&gt;
      &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;任意一种情况对应的条件都是不可能&lt;em&gt;同时&lt;/em&gt;达成的，因为要求的成员数目都大于真正的成员数目，不会产生同一个term两个leader的现象。换句话说，旧配置集群与新配置集群的&lt;em&gt;任意多数派必然有交集&lt;/em&gt;，即：至少存在一个voter（接受旧leader的$C_{new}$，并且为新leader投票），不会出现disjoint majorities。&lt;/p&gt;
&lt;p&gt;因此增减一个server情况，直接切换配置是安全的。&lt;/p&gt;
&lt;p&gt;这个交集也保证了在变更配置的过程中，在$C_{old}$中、以及变更期间复制的日志，最后一定会出现在$C_{new}$。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;何时开始下一次变更&lt;/strong&gt;
能够开始下一次配置更新的&lt;em&gt;前提是当前的配置已经commit&lt;/em&gt;，否则无法保证安全性。如果server在$C_{new}$commit以后才使用$C_{new}$，会带来很多不必要的、额外的维护工作，&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;leader很难知道旧配置集群的多数派使用$C_{new}$的时间。&lt;/li&gt;
&lt;li&gt;需要跟踪哪些server知道了commit，且做持久化。但这些是raft本身不具备的功能。&lt;/li&gt;
&lt;li&gt;如果leader改变了，那么需要移除$C_{new}$的entry，此时，server还需要准备回滚到上一个配置。&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;strong&gt;majority的是对谁而言的&lt;/strong&gt;
对于选举和append entry，都是仅由&lt;em&gt;调用方&lt;/em&gt;来判断是否达成多数派，接收方不负责，否则会存在类似“鸡生蛋蛋生鸡”的问题。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;可用性&lt;/strong&gt;
配置变更给保证集群的可用性带来了几个问题。&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;Catching up new servers
&lt;img src=&#34;http://localhost:1313/images/2019/15662894360490.jpg&#34;
	
	
	
	loading=&#34;lazy&#34;
	
		alt=&#34;-w517&#34;
	
	
&gt;&lt;/p&gt;
&lt;p&gt;一个新server加入集群，新server通常并不包含任何entry，那么可能需要花费较长的时间来同步日志。在这段时间，集群更容易出现不可用的问题。例如：3-&amp;gt;4，此时要求的majority是3，但是s3挂了。&lt;/p&gt;
&lt;p&gt;为了最小化不可用的出现，需要&lt;em&gt;保证不可用的时间在一次election timeout内&lt;/em&gt;。&lt;/p&gt;
&lt;p&gt;&lt;img src=&#34;http://localhost:1313/images/2019/15662902585664.jpg&#34;
	
	
	
	loading=&#34;lazy&#34;
	
		alt=&#34;-w530&#34;
	
	
&gt;&lt;/p&gt;
&lt;p&gt;具体方法是，&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;新加入的server先作为non-voting成员。&lt;/li&gt;
&lt;li&gt;复制到新server的过程分为多个round，每个round都复制leader所有的entry。&lt;/li&gt;
&lt;li&gt;当前复制的round内，leader可能又有新的entry了，下一个round会进行复制。&lt;/li&gt;
&lt;li&gt;在固定round内（例如：10），如果最后一个round的时间 &amp;lt; election timeout，此时假设不存在更多的entry会导致明显的不可用，添加新server。&lt;/li&gt;
&lt;li&gt;否则leader终止变更配置。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Removing the current leader
如果使用joint Consensus，或没有leadership transfer的情况下，需要一个leader下线的方法：旧leader等到$C_{new}$ commit以后让位（转变为follower状态）。&lt;/p&gt;
&lt;p&gt;在commit之前，当前leader管理集群不包含leader自己，复制和投票的时候不把自己算入majority。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Disruptive servers
被排除在$C_{new}$之外的server，由于不再收到heartbeat，会不断的发起投票。虽然新选出的leader始终会出现在$C_{new}$中，但是这干扰了集群正常的工作。&lt;/p&gt;
&lt;p&gt;第一个思路是引入一个Pre-Vote阶段，在发起选举前，检查自己是否有成为leader的资格，即：candidate的log比大多数server更新。但并不总是有效。例如：{ABCD}-&amp;gt;{ABC}的时候，A是Leader，在尝试复制$C_{new}$到BC的时候，D可能发起了Pre-Vote，D的log相对于BC足够新，可以获得BC的投票成为leader。因此检查log的方式是不可行的。&lt;/p&gt;
&lt;p&gt;raft使用的方式是，如果一个server获得上一次heartbeat的时间在最小election timeout内，这个server收到RequestVote时就不更新term或投票。&lt;/p&gt;
&lt;p&gt;如果确实需要发起选举，例如：进行leadership transfer的时候，可以用一个标志位来区分。&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;strong&gt;bug in single-server change&lt;/strong&gt;
如果配置变更是在同一个term内完成的，那么不会有问题。但如果出现在跨term且并发的配置变更，就不一定了。&lt;/p&gt;
&lt;p&gt;例如先后增减一个server，具体过程如下，
&lt;img src=&#34;http://localhost:1313/images/2019/15663835361828.jpg&#34;
	
	
	
	loading=&#34;lazy&#34;
	
		alt=&#34;-w630&#34;
	
	
&gt;&lt;/p&gt;
&lt;p&gt;2中，s1把D复制到s1和s5然后挂了，接着s2接受s2、s3、s4的投票（使用C判断majority）成为term2的leader。5中，s2把E复制到s2和s3，并标记为committed（使用E判断majority）。然后s1恢复，接受s1、s4、s5的投票（使用D判断majority）成为term3的leader，继续复制D，最后在7中覆盖已提交的E。&lt;/p&gt;
&lt;p&gt;这个问题类似&lt;a class=&#34;link&#34; href=&#34;http://localhost:1313/2019/2019-07-15-6.824-Spring-2018-Lecture-5/#%e5%ae%89%e5%85%a8%ef%bc%88Safety%ef%bc%89&#34; &gt;提交上一个term的entry&lt;/a&gt;，解决方法是一样的，&lt;em&gt;leader当选以后，直到当前term的entry提交以后，才能开始下一次配置变更&lt;/em&gt;。可以通过append一个no-op entry来实现。&lt;/p&gt;
&lt;p&gt;原文的single-server change保证了，在同一个term内不会出现未提交的configuration entry。这个patch保证了，来自先前term未提交的configuration entry永远不会被提交。&lt;/p&gt;
&lt;p&gt;回到前面的例子，3中s2成为term2的leader以后立即append no-op entry，此时使用C判断majority，假设复制到s2、s3、s4的index 2。接着s2继续把E复制到s2和s3。如果接下来s1恢复并发生了选举，s1不可能成为leader，因而避免了已提交的E被覆盖的情况。&lt;/p&gt;
&lt;h3 id=&#34;使用joint-consensus&#34;&gt;使用joint consensus
&lt;/h3&gt;&lt;p&gt;这个方法并不建议在工程中使用，更简单的single-server change足以将集群变更为任何期望的配置。&lt;/p&gt;
&lt;p&gt;&lt;img src=&#34;http://localhost:1313/images/2019/15663598511958.jpg&#34;
	
	
	
	loading=&#34;lazy&#34;
	
		alt=&#34;-w344&#34;
	
	
&gt;&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;joint consensus&lt;/strong&gt;
joint consensus状态混合了新旧配置，允许每个server在不同的时间安全地切换配置，且在这个过程中能持续提供服务，这个状态中，&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;entry会被复制到所有新旧配置。&lt;/li&gt;
&lt;li&gt;来自任何配置的机器都可以被选举为leader。&lt;/li&gt;
&lt;li&gt;选举和append的majority，需要分别来自新旧配置。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;相比single-server change，joint consensus引入了一个中间的entry $C_{old,new}$，具体过程是，&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;将新旧配置存储到$C_{old,new}$，并复制，进入joint consensus状态。&lt;/li&gt;
&lt;li&gt;每个server收到$C_{old,new}$后&lt;em&gt;立即生效&lt;/em&gt;，leader使用$C_{old,new}$来判断是否提交。
$C_{old,new}$复制的过程中，如果leader挂了，那么新的leader可能在$C_{old,new}$或$C_{old}$中选举出。无论leader来自哪个配置，$C_{new}$不能单方进行决策。&lt;/li&gt;
&lt;li&gt;$C_{old,new}$提交后，leader可以复制$C_{new}$。
一旦$C_{old,new}$提交，$C_{new}$或$C_{old}$都不能单方进行决策。&lt;/li&gt;
&lt;li&gt;leader使用$C_{new}$来判断是否提交，提交后，完成配置变更。&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;strong&gt;安全性&lt;/strong&gt;
在joint consensus过程中，发生选举时，可能从以下情况选出leader（按joint consensus的步骤顺序列举），&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;来自$C_{old}$，log不包含$C_{old,new}$。&lt;/li&gt;
&lt;li&gt;来自$C_{old}$，log包含$C_{old,new}$。&lt;/li&gt;
&lt;li&gt;来自$C_{new}$，log包含$C_{old,new}$。&lt;/li&gt;
&lt;li&gt;来自$C_{new}$，log包含$C_{new}$。&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;而任何两个leader的组合都是不可能同时出现的。&lt;/p&gt;
&lt;table&gt;
  &lt;thead&gt;
      &lt;tr&gt;
          &lt;th&gt;leader组合&lt;/th&gt;
          &lt;th&gt;不可能出现的原因&lt;/th&gt;
      &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
      &lt;tr&gt;
          &lt;td&gt;1+1 or 4+4&lt;/td&gt;
          &lt;td&gt;选举规则限制&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
          &lt;td&gt;1+2&lt;/td&gt;
          &lt;td&gt;先看2的选举，需要分别来自新旧配置的多数派，此时已经不能再从$C_{old}$中选举1&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
          &lt;td&gt;1+3&lt;/td&gt;
          &lt;td&gt;类似1+2&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
          &lt;td&gt;1+4&lt;/td&gt;
          &lt;td&gt;a. 先看4，既然$C_{new}$出现了，那么$C_{old,new}$肯定提交了，这个提交需要分别来自新旧配置的多数派，因此$C_{old}$中不包含$C_{old,new}$的server无法选举为leader&lt;!-- raw HTML omitted --&gt;b. $C_{new}$是在$C_{old,new}$提交后才复制，如果选举出4，1就不会存在&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
          &lt;td&gt;2+2&lt;/td&gt;
          &lt;td&gt;类似1+2&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
          &lt;td&gt;2+3&lt;/td&gt;
          &lt;td&gt;类似1+2&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
          &lt;td&gt;2+4&lt;/td&gt;
          &lt;td&gt;类似1+2&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
          &lt;td&gt;3+3&lt;/td&gt;
          &lt;td&gt;类似1+2&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
          &lt;td&gt;3+4&lt;/td&gt;
          &lt;td&gt;类似1+2&lt;/td&gt;
      &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;因此不会出现disjoint majorities的情况。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;是否受single-server change的bug影响&lt;/strong&gt;
不受。&lt;/p&gt;
&lt;h2 id=&#34;日志压缩&#34;&gt;日志压缩
&lt;/h2&gt;&lt;p&gt;raft的日志随着客户端不断的请求增长。一旦entry已经提交并执行，那么中间的状态和操作就不再需要，可以被压缩。&lt;/p&gt;
&lt;p&gt;文章讨论了几种进行日志压缩的方法，&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Snapshotting memory-based state machines&lt;/li&gt;
&lt;li&gt;Snapshotting disk-based state machines&lt;/li&gt;
&lt;li&gt;Incremental cleaning approaches&lt;/li&gt;
&lt;li&gt;Leader-based approaches&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;这几个方法有一些核心概念基本都是相通的，&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;每个server独立的负责日志压缩，而非由leader集中决定。&lt;/li&gt;
&lt;li&gt;raft向状态机转移维护prefix of the log的职责。&lt;/li&gt;
&lt;li&gt;raft丢弃部分日志前缀a prefix of the log后，状态机会承担两个新的职责
&lt;ol&gt;
&lt;li&gt;如果server重启，状态机在apply entry前，需要先load那些被丢弃的日志。&lt;/li&gt;
&lt;li&gt;为了落后较多的server或新server能够追上，状态机可能需要输出一个状态的镜像。&lt;/li&gt;
&lt;/ol&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;h3 id=&#34;snapshotting-memory-based-state-machines&#34;&gt;Snapshotting memory-based state machines
&lt;/h3&gt;&lt;p&gt;适用于状态机的数据是存放在内存的情况。每个server独立的创建已经提交entry的snapshot。主要过程是，&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;状态机序列化当前状态。&lt;/li&gt;
&lt;li&gt;一旦状态机完成snapshot的写入以后，日志就可以被截断了，raft首先保存snapshot中lastIncludedIndex和lastIncludedTerm，以及这个index对应的lastIncludedConf。&lt;/li&gt;
&lt;li&gt;raft可以丢弃截止index的entry和先前的snapshot。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;img src=&#34;http://localhost:1313/images/2019/15663712160542.jpg&#34;
	
	
	
	loading=&#34;lazy&#34;
	
		alt=&#34;-w318&#34;
	
	
&gt;&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;InstallSnapshot&lt;/strong&gt;
为了落后较多的server或新server能够追上，这个方法里使用InstallSnapshot来实现。leader仅当丢弃了需要复制的next entry的时候，才发送snapshot，snapshot以chunks的形式有序发送。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;并发创建&lt;/strong&gt;
创建snapshot时，状态机需要维持一个不变的状态，但进行序列化和落盘需要较长的时间，因此创建的过程需要与普通操作并发执行。可以使用copy-on-write实现，有两种方法，&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;状态机使用不可变数据结构。&lt;/li&gt;
&lt;li&gt;依赖os的copy-on-write支持，例如：linux的fork。&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;copy-on-write占用额外的内存，在创建的过程中，占用的额外内存与状态的修改成正相关，因此需要事先计划和管理。如果在snapshot的过程中，内存满了，那么server只能暂停服务，此时集群可能还是可用的。最好不要终止稍后重试，下次创建的时候很可能还会有类似的问题。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;何时创建&lt;/strong&gt;
如果创建的过于频繁，会浪费磁盘带宽和其他资源，如果过于稀少，会导致创建出过大的snapshot，增加传输和回放的时间。&lt;/p&gt;
&lt;p&gt;有这么几个判断的方法，&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;如果size(log)明显大于一个预定的值。
当这个值明显大于snapshot的大小时，磁盘写入开销会很小。但对于较小的状态机，需要等待较长的时间才会有满足大小要求的log。&lt;/li&gt;
&lt;li&gt;如果size(log)大于size(snapshot)的倍数。
不过判断当前状态机的snapshot大小并不容易。&lt;/li&gt;
&lt;li&gt;如果size(log)大于size(prev snapshot)的倍数，expansion factor。
expansion factor控制了磁盘带宽的开销。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;还可以仅在少数派server上创建snapshot，不影响服务client。&lt;/p&gt;
&lt;h2 id=&#34;client交互&#34;&gt;Client交互
&lt;/h2&gt;&lt;h3 id=&#34;查找cluster&#34;&gt;查找cluster
&lt;/h3&gt;&lt;p&gt;如果配置固定，这个过程很简单。难点在于成员不断变更的情况，可用的方法有，&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;广播，但受限于特定的网络环境。&lt;/li&gt;
&lt;li&gt;使用外部的目录服务，例如：DNS。需要在变更的过程中增减相应的server。&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;路由请求到leader&#34;&gt;路由请求到leader
&lt;/h3&gt;&lt;p&gt;client的请求是由leader处理的，因此client需要找到leader，可以随机的选取一个server发起请求，如果不是leader，server拒绝，client重试直到找到为止，尝试次数期望是$(n+1)/2$。在此基础上可以做一些优化，&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;server拒绝的时候返回leader。&lt;/li&gt;
&lt;li&gt;server做代理，转发请求到leader。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;还需要避免过期的leader信息导致处理client请求的时候产生不必要的延迟，&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;leader：如果产生网络分区，且client向拥有少数派的leader发送了请求，在分区恢复前，这个请求一直都无法得到处理。因此当&lt;em&gt;超过election timeout以后，leader都没有向多数派成功的发送心跳，那么leader让位&lt;/em&gt;。&lt;/li&gt;
&lt;li&gt;follower：如果follower发起新的选举或者term变更，那么follower丢弃当前维护的leader信息。&lt;/li&gt;
&lt;li&gt;client：当丢失与某个server的连接，应该随机选取一个server进行重试。&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;线性一致性&#34;&gt;线性一致性
&lt;/h3&gt;&lt;p&gt;截止目前，raft提供了at-least-once的语义。client重试、以及网络导致请求重复会导致命令被执行多次。但是at-least-once对于一个基于共识的系统是不够的，raft需要&lt;em&gt;可线性化的语义&lt;/em&gt;，通过对请求去重，可以实现这一点。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;使用session去重&lt;/strong&gt;
每个client分配一个唯一id，每个请求分配一个唯一的递增序号。server维护每个client的session，这个session跟踪了每个client的最新序号和对应的response，如果收到了一个已经处理过的序号，那么直接返回。&lt;/p&gt;
&lt;p&gt;这样每个命令就做到了以log中第一次出现的顺序立即生效且只执行一次。&lt;/p&gt;
&lt;p&gt;对于来自同一个client的并发请求，server维护一个&amp;lt;序号，response&amp;gt;的集合。每个请求中携带一个client未收到的最小序号，server丢弃小于这个序号的response。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;session保存的多久&lt;/strong&gt;
受存储的限制，session不能永久保存，server需要对何时过期session达成共识。&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;一个方法是设置存储session数的上限，并使用lru淘汰session。&lt;/li&gt;
&lt;li&gt;另一个方法是基于对时间源达成的共识来淘汰session（原文中的描述不是非常清晰，待补充）。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;处理session过期的client请求&lt;/strong&gt;
当session过期后，client还继续操作时，这被看做异常情况（待补充）。&lt;/p&gt;
&lt;h3 id=&#34;更高效的处理read-only请求&#34;&gt;更高效的处理read-only请求
&lt;/h3&gt;&lt;p&gt;raft日志的目的是以相同的顺序把变更复制到server上，并保证读写时候的线性一致性语义。read-only命令只涉及查询状态机，可以绕开日志的复制，避免同步磁盘写入，会大大的提升性能。但如果没有额外的控制，client会读取到过期的值。&lt;/p&gt;
&lt;p&gt;为了使得绕开raft日志的read-only请求保持线性一致性，针对每次read-only请求，leader需要，&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;如果当前term还没有提交过entry，等待直到有。如果是刚成为leader，则需要先提交一个no-op entry。&lt;/li&gt;
&lt;li&gt;leader将当前的commit index记录到本地变量&lt;em&gt;readIndex&lt;/em&gt;，这个会作为read-only操作的下界。&lt;/li&gt;
&lt;li&gt;leader需要自己的身份是有效的，不存在在自己不知情的情况下（网络分区）选举出了新的leader。这里的方法与&lt;a class=&#34;link&#34; href=&#34;#%e8%b7%af%e7%94%b1%e8%af%b7%e6%b1%82%e5%88%b0leader&#34; &gt;路由请求到leader&lt;/a&gt;中避免过期的leader类似，如果成功向多数派发送了heartbeat，那么leader可以知道在发送heartbeat的时候，身份仍然是有效的。&lt;/li&gt;
&lt;li&gt;leader等待lastApplied &amp;gt;= readIndex，此时的readIndex是&lt;em&gt;能保证线性一致性的最小index&lt;/em&gt;。&lt;/li&gt;
&lt;li&gt;leader向自己的状态机发起查询请求，并返回结果。&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;strong&gt;优化leadership确认&lt;/strong&gt;
每次查询请求都需要执行3，可以把所有累计的查询通过一次heartbeat来确认leadership。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;follower分担read-only负载&lt;/strong&gt;
同样需要保证不读取到过期的数据，保证线性一致性。为此follower可以向leader发送一个&lt;em&gt;查询当前readIndex的请求&lt;/em&gt;，然后leader执行上面的1-3，follower执行4-5。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;使用时钟减少heartbeat带来的延迟&lt;/strong&gt;
虽然有batch优化，read-only查询仍然需要做一次heartbeat来确认leadership，可以用时钟来避免heartbeat带来的延迟。&lt;/p&gt;
&lt;p&gt;&lt;img src=&#34;http://localhost:1313/images/2019/15668148723093.jpg&#34;
	
	
	
	loading=&#34;lazy&#34;
	
		alt=&#34;-w262&#34;
	
	
&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;leader使用heartbeat来维持一个lease，如果leader成功向多数派发送了heartbeat，那么leader可以认为在接下来的election time时间内都不会有新的leader产生，这个lease可以扩展到$start+\frac{\textit{election timeout}}{\textit{clock drift bound}}$，在这个时间之前都不用执行上面的步骤3。&lt;/li&gt;
&lt;li&gt;在进行leadership transfer的时候需要将lease主动过期，因为会导致更早的产生新leader。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;em&gt;要注意的是&lt;/em&gt;，使用lease的方式假设了server之间时钟漂移的上界（在给定的一段时间内，没有server的时钟增加的时间会超过这个上界），找到并维护这个值会增加额外的运维成本。如果假设失效了，系统可能会返回任意过期的信息。&lt;/p&gt;
&lt;p&gt;可以使用一个的扩展来增强对client提供的保证，&lt;em&gt;即使上述假设失效的情况下，读操作满足线性一致性&lt;/em&gt;，不至于错的离谱。具体方法是，&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;server返回client的时候，带上状态机状态对应的index。&lt;/li&gt;
&lt;li&gt;client跟踪自己看到的与结果对应的最新index，发送请求的时候带上这个index。&lt;/li&gt;
&lt;li&gt;如果server收到的index &amp;gt; lastApplied，那么server暂时不处理这次请求。&lt;/li&gt;
&lt;/ol&gt;
&lt;h2 id=&#34;问题&#34;&gt;问题
&lt;/h2&gt;&lt;ol&gt;
&lt;li&gt;创建snapshot的时候，有什么限制？
&lt;ul&gt;
&lt;li&gt;不能丢弃未提交的和未执行的&lt;/li&gt;
&lt;li&gt;已执行的entry可能需要用于使其他server更新&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;snapshot和log的关系？
snapshot反映的是已经执行的log。&lt;/li&gt;
&lt;li&gt;server持久化了哪些数据在磁盘上？
&lt;ul&gt;
&lt;li&gt;截止到某个entry的snapshot + 后续的log = server完整的log。&lt;/li&gt;
&lt;li&gt;其他状态信息，如：currentTerm，votedFor等。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;如果某个follower落后了，同时leader丢弃了follower的所需的log，怎么办？
nextIndex将无法会退到那个entry，leader会使用InstallSnapshot RPC。&lt;/li&gt;
&lt;li&gt;leader何时会向落后follower发送InstallSnapshot RPC？
上面的问题即为答案。&lt;/li&gt;
&lt;li&gt;为何leader不仅仅丢弃所有follower都有的entry？
&lt;ul&gt;
&lt;li&gt;每个server是独立创建snapshot的。&lt;/li&gt;
&lt;li&gt;少数落后或失败的follower会导致leader log的持续增加。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;snapshot包含什么信息？
&lt;ul&gt;
&lt;li&gt;term&lt;/li&gt;
&lt;li&gt;lastIncludedIndex&lt;/li&gt;
&lt;li&gt;lastIncludedTerm of lastIncludedIndex&lt;/li&gt;
&lt;li&gt;lastIncludedConf at lastIncludedIndex&lt;/li&gt;
&lt;li&gt;snapshot data&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;follower InstallSnapshot RPC的流程是什么？
&lt;ul&gt;
&lt;li&gt;检查term&lt;/li&gt;
&lt;li&gt;检查是否已包含lastIncludedIndex/lastIncludedTerm&lt;/li&gt;
&lt;li&gt;set lastApplied = lastIncludedIndex，写入data&lt;/li&gt;
&lt;li&gt;使用snapshot重置状态机&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;server收到InstallSnapshot RPC以后，有没有可能会导致状态机的状态回退？
不会。follower会检查是否已经包含lastIncludedIndex/lastIncludedTerm。&lt;/li&gt;
&lt;li&gt;为什么在处理read-only请求的时候需要提交一个no-op entry？
问题类似于&lt;a class=&#34;link&#34; href=&#34;http://localhost:1313/2019/2019-07-15-6.824-Spring-2018-Lecture-5/#%e5%ae%89%e5%85%a8%ef%bc%88Safety%ef%bc%89&#34; &gt;提交上一个term的entry&lt;/a&gt;，新leader并不知道先前term的entry是否已提交。需要append一个no-op entry，如果成功提交，那么表示在此之前的所有entry都是已提交了的。&lt;/li&gt;
&lt;li&gt;配置变更时，从集群移除的server如果发起选举，会[影响集群的可用性](#single-server change)，为何不直接把离开集群的server关闭？
$C_{new}$不会复制到那些离开集群的server，因此无法做到$C_{new}$提交以后，就立即下线这些server。在关闭前的这段时间里，这些server可能会影响集群的可用性。&lt;/li&gt;
&lt;li&gt;joint consensus过程中，选举和提交需要同时获得新旧配置的多数派，这对性能的影响有多大？
&lt;ul&gt;
&lt;li&gt;在大多数不发生错误的情况下，获得新旧配置的多数派应该是一个比较快的过程。&lt;/li&gt;
&lt;li&gt;获得新旧配置的多数派仍然会比普通的commit要慢，但考虑到配置变更并不经常发生，所以这个代价可以忍。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;joint consensus过程中，选举和提交需要同时获得新旧配置的多数派是否是必须的？
是，这是为了确保安全性所必须的。在joint consensus关于[安全性的讨论中](#使用joint consensus)，列举了如果leader失败，发生选取时的情况，除特殊的两个外，获得新旧配置多数派的要求&lt;em&gt;避免了disjoint majority出现&lt;/em&gt;。&lt;/li&gt;
&lt;li&gt;配置变更时，新server是作为non-voting成员加入的，这个要求为何可以提升可用性？
当$C_{old,new}$提交以后，集群才可以继续处理请求。而$C_{old,new}$的提交需要新配置的多数派复制成功，空server会拖慢达这个过程。&lt;/li&gt;
&lt;li&gt;离开集群的server发起投票会影响集群的可用性，为何不直接使用当前配置来判断，看发起请求的server是否在配置中？&lt;/li&gt;
&lt;li&gt;joint consensus的起止时刻是什么？
&lt;ul&gt;
&lt;li&gt;开始：leader append $C_{old,new}$。&lt;/li&gt;
&lt;li&gt;终止：
&lt;ul&gt;
&lt;li&gt;leader未成功提交$C_{old,new}$就挂了。&lt;/li&gt;
&lt;li&gt;leader成功提交$C_{new}$。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;配置的entry是否可能被后续leader覆盖？
可能。如果前任leader未成功提交$C_{old,new}$就挂了。&lt;/li&gt;
&lt;li&gt;如果log和创建的snapshot大小差别不大，那snapshot是否还有用（例如：k/v server大量插入新key）？
有。
&lt;ul&gt;
&lt;li&gt;避免raft log entry一直占用内存。&lt;/li&gt;
&lt;li&gt;恢复服务时，使用snapshot可能会比直接使用log能更快（比如snapshot数据的组织方式更好）。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;InstallSnapshot会占用带宽不？
会，如果状态很大的话。可以用一些方式来减少InstallSnapshot RPC的调用，
&lt;ul&gt;
&lt;li&gt;考虑让leader保留更久的log，来应对follower的lag或暂时的下线。&lt;/li&gt;
&lt;li&gt;只发送两个server diff的部分。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;follower的entry是否有可能不在收到的snapshot里面？
有可能，例如：leader尚未提交的entry。&lt;/li&gt;
&lt;li&gt;InstallSnapshot是否是原子的？如果在InstallSnapshot执行途中，follower挂了，重发InstallSnapshot是否ok？
是原子的、幂等的。&lt;/li&gt;
&lt;/ol&gt;
&lt;h1 id=&#34;lecture&#34;&gt;Lecture
&lt;/h1&gt;&lt;h2 id=&#34;性能问题&#34;&gt;性能问题
&lt;/h2&gt;&lt;p&gt;raft牺牲了性能来换取简洁的设计：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;follower拒绝乱序的append，不允许日志有空洞。&lt;/li&gt;
&lt;li&gt;尚未支持batch或pipeline方式的append。&lt;/li&gt;
&lt;li&gt;对于大的状态，snapshot比较浪费。&lt;/li&gt;
&lt;li&gt;慢leader会影响系统的性能。&lt;/li&gt;
&lt;/ol&gt;
&lt;h1 id=&#34;references&#34;&gt;References
&lt;/h1&gt;&lt;ol&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;http://loopjump.com/raft_one_server_reconfiguration/&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;Raft One-Server成员变更&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://zhuanlan.zhihu.com/p/30706032&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;一文看尽 Raft 一致性协议的关键点&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://gist.github.com/ongardie/a11f32b70581e20d6bcd&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;ongardie/raft-single-server-changes-safety&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://groups.google.com/forum/#!topic/raft-dev/t4xj6dJTP6E&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;bug in single-server membership changes&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://github.com/peterbourgon/raft/blob/master/JOINT-CONSENSUS.md&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;raft/JOINT-CONSENSUS.md&lt;/a&gt;&lt;/li&gt;
&lt;/ol&gt;
</description>
        </item>
        <item>
        <title>6.824 2018 Lecture 5 Fault Tolerance Raft</title>
        <link>http://localhost:1313/posts/2019/2019-07-15-6.824-spring-2018-lecture-5/</link>
        <pubDate>Mon, 15 Jul 2019 15:13:32 +0000</pubDate>
        
        <guid>http://localhost:1313/posts/2019/2019-07-15-6.824-spring-2018-lecture-5/</guid>
        <description>&lt;h1 id=&#34;readings---in-search-of-an-understandable-consensus-algorithm-extended-version-to-end-of-section-5论文&#34;&gt;Readings - In Search of an Understandable Consensus Algorithm (Extended Version) (to end of Section 5)论文
&lt;/h1&gt;&lt;h2 id=&#34;introduction&#34;&gt;Introduction
&lt;/h2&gt;&lt;p&gt;共识算法（Consensus algorithms）允许一组机器作为一个一致的组工作，这个组可以在某些成员失败的情况下存活。Paxos是过去10多年最常被讨论的共识算法，但是难以理解且不便于实现。提出Raft的主要目标是可理解性。通过解耦leader选举、log复制和安全，以及减少状态空间，来增加可理解性。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;多副本状态机（Replicated state machine）与共识算法&lt;/strong&gt;
共识算法常常出现在多副本状态机的讨论中。&lt;/p&gt;
&lt;p&gt;&lt;img src=&#34;http://localhost:1313/images/2019/15633494887544.jpg&#34;
	
	
	
	loading=&#34;lazy&#34;
	
		alt=&#34;-w384&#34;
	
	
&gt;&lt;/p&gt;
&lt;p&gt;多副本状态机常用于解决分布式系统中的各种容错问题，使用复制log来实现，每个状态机以相同的顺序执行相同的命令，最终算出相同的状态和结果。保证replicated log的一致是由共识算法来实现的。&lt;/p&gt;
&lt;p&gt;共识算法一般有以下几个特征：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;安全：&lt;em&gt;非拜占庭条件&lt;/em&gt;（网络问题、丢包、乱序）下，不返回任何错误的结果。&lt;/li&gt;
&lt;li&gt;可用性：大多数server可用，则系统整体可用。&lt;/li&gt;
&lt;li&gt;不依赖时间来保证日志的一致性。&lt;/li&gt;
&lt;li&gt;大多数server执行完毕则返回，少量慢server不影响系统性能。&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id=&#34;raft&#34;&gt;Raft
&lt;/h2&gt;&lt;p&gt;raft的工作过程是，首先选举leader，leader会负责管理replicated log。leader接受来自client的log，复制到其他机器，并在安全的时候通知其他机器把log输入状态机。&lt;/p&gt;
&lt;p&gt;raft可以解耦为以下三个部分：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;leader选举（Leader election）&lt;/li&gt;
&lt;li&gt;日志复制（Log replication）&lt;/li&gt;
&lt;li&gt;安全（Safety）&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;在理解这三个部分前，有几个基本概念需要知道，
&lt;strong&gt;server 状态&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;leader：只能有一个leader&lt;/li&gt;
&lt;li&gt;follower：只响应来自其他server的请求&lt;/li&gt;
&lt;li&gt;candidate：用于选举&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;img src=&#34;http://localhost:1313/images/2019/15633584684199.jpg&#34;
	
	
	
	loading=&#34;lazy&#34;
	
		alt=&#34;-w433&#34;
	
	
&gt;&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;时间&lt;/strong&gt;
raft把时间分为terms，term由一个递增的数字标识，表示&lt;em&gt;current term&lt;/em&gt;。每个term以leader选举作为起始，如果未选举出leader，那么下一个term继续选。term内选举出的leader管理集群直到term结束。&lt;/p&gt;
&lt;p&gt;&lt;img src=&#34;http://localhost:1313/images/2019/15633589391342.jpg&#34;
	
	
	
	loading=&#34;lazy&#34;
	
		alt=&#34;-w351&#34;
	
	
&gt;&lt;/p&gt;
&lt;p&gt;不同的server可能观察到不同的term转换，server使用current term（在server通信的时候发送）作为逻辑时钟，来确保能够探测到过期的信息。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;通信&lt;/strong&gt;
使用rpc。基本的共识算法使用两种rpc，&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;RequestVote RPCs：candidates调用，来进行拉票，用于选主。&lt;/li&gt;
&lt;li&gt;AppendEntries RPCs：leader发起，用于复制日志和心跳。&lt;/li&gt;
&lt;/ol&gt;
&lt;h3 id=&#34;leader选举leader-election&#34;&gt;leader选举（Leader election）
&lt;/h3&gt;&lt;p&gt;选举过程为，follower在election timeout内都没有收到leader的通信，则转换为candidate状态，并自增当前term、为自己投票、向其他server发送RequestVote RPCs。candidate维持在这个状态，直到下列情况出现：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;获得了相同term的多数派的投票，赢得选举。
一旦candidate赢得选举，就转换为leader，并向其他server发送heartbeat，宣告leadership。&lt;/li&gt;
&lt;li&gt;收到AppendEntries RPCs，其他candidate赢得了选举，成为leader。
&lt;ol&gt;
&lt;li&gt;如果leader的term &amp;gt;= 自己的term，那么这个leader是合法的，自己转换为follower状态。&lt;/li&gt;
&lt;li&gt;如果leader的term &amp;lt; 自己的term，那么拒绝这个rpc，自己维持在candidate状态。&lt;/li&gt;
&lt;/ol&gt;
&lt;/li&gt;
&lt;li&gt;election timeout后，未获得足够的选票。
多个candidate可能同时发起选举，发生了split vote，导致任何一个都无法获得多数派的投票。自增term，开始下一次选举。&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;在下面的状态图中，状态转换的上方表示触发转换的event，下方表示发生转换时执行的action。&lt;/p&gt;
&lt;p&gt;&lt;img src=&#34;http://localhost:1313/images/2019/leader%20election.jpg&#34;
	
	
	
	loading=&#34;lazy&#34;
	
		alt=&#34;leader election&#34;
	
	
&gt;&lt;/p&gt;
&lt;p&gt;选举安全性：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;在每个term内，每个server只能至多为一个candidate投票。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;在选举过程中，raft使用随机election timeout来保证，&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;split votes（没有任何candidate能获得大多数投票）会很少出现。
比较常见的是，C先timeout，然后投票给自己，并发送RequestVote RPCs，最终win。
&lt;img src=&#34;http://localhost:1313/images/2019/15634274004241.jpg&#34;
	
	
	
	loading=&#34;lazy&#34;
	
		alt=&#34;-w397&#34;
	
	
&gt;&lt;/li&gt;
&lt;li&gt;且split votes能够被快速解决。如果出现split votes，candidate先等待随机election timeout。&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;日志复制log-replication&#34;&gt;日志复制（Log replication）
&lt;/h3&gt;&lt;p&gt;&lt;strong&gt;如何复制&lt;/strong&gt;&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;leader追加log entry。&lt;/li&gt;
&lt;li&gt;AppendEntries RPC。&lt;/li&gt;
&lt;li&gt;当log entry被安全复制（成为&lt;em&gt;committed&lt;/em&gt; log entry，leader更新commit index），
&lt;ul&gt;
&lt;li&gt;leader apply log entry，然后返回结果到client。&lt;/li&gt;
&lt;li&gt;leader AppendEntries RPC通知follower committed entry，follower apply log entry。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;如果follower失败、运行缓慢、丢包，leader重试AppendEntries RPC（就算leader已经返回client结果），直到所有follower最终收到所有log entry。raft保证了committed entry是&lt;em&gt;持久化的&lt;/em&gt;，且最终能被所有可用的状态机执行。&lt;/p&gt;
&lt;p&gt;那什么时候才算log entry被安全复制？当创建log entry的leader把entry复制到大多数server上以后。这同时也提交了在leader中所有先前的log，包括被前leader创建的log。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;应对出错&lt;/strong&gt;
raft在复制log时会维护如下性质来保证Log Matching Property（log consistency），&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;如果两个entry在不同的log中有相同的index和term，那么它们的cmd是相同的。
因为leader至多只创建一个带有相同index和term的entry，且entry不会在log中改变位置。&lt;/li&gt;
&lt;li&gt;如果两个entry在不同的log中有相同的index和term，那么所有先前的log都是相同的。
consistency check：AppendEntries RPC发送新的entry时，会包含前一个entry，如果follower没有找到与前一个entry相同的index和term，那么follower不会写入新的entry。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;根据以上两点，结合数学归纳法可知，Log Matching Property可以被满足。在正常情况下，leader和follower的log是保持一致的。&lt;/p&gt;
&lt;p&gt;如果leader或follower崩溃引起了log不一致，如何恢复一致性？leader会强制follower复制自己的log，&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;leader维护了&lt;code&gt;{follower: nextIndex}&lt;/code&gt;，表示下一个将会发送到follower的entry。&lt;/li&gt;
&lt;li&gt;leader启动时，将&lt;code&gt;nextIndex&lt;/code&gt;置为&lt;code&gt;max(self log index) + 1&lt;/code&gt;。&lt;/li&gt;
&lt;li&gt;如果follower的日志不一致，那么接下来的AppendEntries RPC consistency check会失败。leader会将&lt;code&gt;nextIndex - 1&lt;/code&gt;并重试，直到成功。&lt;/li&gt;
&lt;/ol&gt;
&lt;h3 id=&#34;安全safety&#34;&gt;安全（Safety）
&lt;/h3&gt;&lt;p&gt;任何实现了日志复制的系统，都必须满足：一旦entry已经applied到状态机，那么任何其他状态机都不能为这条entry apply一个不同的值。&lt;/p&gt;
&lt;p&gt;截止目前所描述的leader选举，存在的问题是，如果follower未收到leader的提交，那么当它成为leader以后，就会用自己的log覆盖其他server的。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;选举约束&lt;/strong&gt;
任何基于leader的共识算法，leader必须最终包含所有committed entry。raft需要保证所有committed entry，都出现在后续每个新的leader中，而这个保证确保了本节内容开头的安全性要求。&lt;/p&gt;
&lt;p&gt;在选举的时候，使用voting process来避免未包含所有committed entry的candidate赢得选举。由于candidate在选举的时候需要联系大多数server，即只需要求每个committed entry必须出现在至少一个server（选举时联系的server）里面即可（抽屉原理）。&lt;/p&gt;
&lt;p&gt;如果candidate包含的日志与大多数server包含的log&lt;em&gt;至少一样新&lt;/em&gt;，那么这个candidate就包含了所有的log。具体判断方法是，&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;在RequestVote RPC进行投票时，rpc包含了candidate最后一个entry的index和term。&lt;/li&gt;
&lt;li&gt;如果voter（收到rpc call的一方）的log是更新的，即，
$$(lastTerm_v &amp;gt; lastTerm_c) || ((lastTerm_v == lastTerm_c) \&amp;amp;\&amp;amp; (lastIndex_v &amp;gt; lastIndex_c))$$
那么voter拒绝此次投票。&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;strong&gt;提交上一个term的entry&lt;/strong&gt;
新leader能否直接提交上一个term的entry？&lt;/p&gt;
&lt;p&gt;&lt;img src=&#34;http://localhost:1313/images/2019/15637956361681.jpg&#34;
	
	
	
	loading=&#34;lazy&#34;
	
		alt=&#34;-w463&#34;
	
	
&gt;&lt;/p&gt;
&lt;p&gt;c中s1成为term4的leader后继续复制term2的日志index2，此时日志已经复制到大多数机器上。按照之前的规则，s1可以认为日志已经是committed。但如果接着s5成为了leader（接受s3和s4的投票），s5会覆盖s1已经commit的日志。&lt;/p&gt;
&lt;p&gt;因此不能仅通过副本数判断先前term的日志是不是committed，还需要满足&lt;em&gt;至少有一个属于当前term的log也复制到大多数server&lt;/em&gt;，才能认为是。&lt;/p&gt;
&lt;h3 id=&#34;follower和candidate崩溃&#34;&gt;follower和candidate崩溃
&lt;/h3&gt;&lt;p&gt;raft rpc是幂等的，leader无限重试。&lt;/p&gt;
&lt;h3 id=&#34;时间和可用性&#34;&gt;时间和可用性
&lt;/h3&gt;&lt;p&gt;raft的安全性不依赖时间。但是系统的可用性依赖时间，这里有尤其指的是leader选举。election timeout需要满足下面的要求，raft才能选举并保持一个稳定的leader，&lt;/p&gt;
&lt;p&gt;$$broadcastTime \ll electionTimeout \ll MTBF$$&lt;/p&gt;
&lt;h3 id=&#34;leadership-transfer&#34;&gt;Leadership transfer
&lt;/h3&gt;&lt;p&gt;本质上还是使用leader选举的机制，通过主动触发目标server进行选举来实现来迁移，与后面的配置变更不同。过程如下：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;旧leader暂停服务。&lt;/li&gt;
&lt;li&gt;旧leader进行正常的日志复制过程，把log复制到目标server。&lt;/li&gt;
&lt;li&gt;旧leader发送TimeoutNow到目标server，触发目标server开始选举（相当于调快了timer，触发了election timeout）。&lt;/li&gt;
&lt;li&gt;目标server大概率会比其他server先发起选举，并成为下一个term的leader。&lt;/li&gt;
&lt;li&gt;选举成功后，旧leader收到heartbeat，出让leadership，至此transfer完成。&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;如果迁移过程未在一个election timeout内完成，那么终止迁移。如果迁移成功了，但是旧leader误以为失败了，那最坏情况是进行一次额外的选举。&lt;/p&gt;
&lt;h2 id=&#34;问题&#34;&gt;问题
&lt;/h2&gt;&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;日志复制时每个步骤都有可能出现错误，如何处理？
raft并不允许“false positives”的出现（返回client执行成功了，但实际上没有成功），但是可能会有“false negatives”的出现，告知client失败了，但实际上是成功了。为了避免false negatives，client必须重试直到成功，且每次只能有一个未完成的command，相应地，raft也必须提供去重机制。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;为什么不能直接复制上一个term未committed的entry？&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;raft为何能够防止脑裂？
2f+1个server，成为leader需要获得大多数server（f+1个）的投票。如果发生网络分区，必然有一个partition的分区少于f+1个。这个分区里面，&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;如果没有leader，那么也不可能选举出leader。&lt;/li&gt;
&lt;li&gt;如果有leader，由于日志无法被安全复制，因此不会对client做出任何承诺（不会成为committed entry，也不会被apply到状态机）。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;为什么需要leader？&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;简化复制日志的过程。&lt;/li&gt;
&lt;li&gt;保证所有的副本都以相同的顺序执行相同的命令。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;server变为candidate后会发生什么？
开始选举：inc term，vote self，发送RequestVote RPC。可能结果如下，&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;赢得选举。
变为leader。&lt;/li&gt;
&lt;li&gt;选举失败，收到来自其他leader的AppendEntries RPC。
变为follower。&lt;/li&gt;
&lt;li&gt;既没有赢得选举，也没有收到来自其他leader的AppendEntries RPC。
维持candidate。election timeout，开始下一次选举。
如果一直无法获得大多数server的投票，这个过程会一直重复，term会一直增加。
&lt;em&gt;此时，如果client有请求会发生什么？&lt;/em&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;如何保证同一个term内只有一个leader？&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;leader需要获得来自大多数server的投票。&lt;/li&gt;
&lt;li&gt;在同一个term内，每个candidate只能投票一次。&lt;/li&gt;
&lt;li&gt;在同一个term内，至多只有一个server能获得大多数server的投票（即使出现网络分区）。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;什么情况下选举会失败？&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;大多数server都失败了。&lt;/li&gt;
&lt;li&gt;两个server获得了相同票数（例如：2f+1个server，挂了一个server）。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;选举失败会发生什么？
election timeout，开始下一次选举。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;如何设置election timeout？&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;$broadcastTime \ll electionTimeout \ll MTBF$&lt;/li&gt;
&lt;li&gt;random&lt;/li&gt;
&lt;li&gt;至少是几个heartbeat间隔&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;replicated和committed entry的区别？&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;replicated是做了复制，可能被覆盖。&lt;/li&gt;
&lt;li&gt;committed，已提交，不会丢失。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;每个副本的日志会完全一致吗？
不会，可能落后，可能会有临时的不一致。但最终会一致，且状态机执行的命令是一致的。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;为什么leader不能直接提交上一个term的entry？以及如何避免直接提交带来的问题？
提交时，需要等到至少一个当前term的entry也安全复制以后。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;leader在什么时候覆盖follower的entry是合法的？
uncommitted entry。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;leader覆盖follower的entry时，可能会覆盖committed entry吗？
不会。&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;先看entry成为committed时，发生了什么。log entry需要安全复制，即发送到大多数server上。&lt;/li&gt;
&lt;li&gt;candidate成为leader，需要联系大多数server，以保证自身的log与大多数server的一样新（选举约束）。因此成为leader的candidate一定包含了所有committed entry。&lt;/li&gt;
&lt;/ol&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;如果下图中的leader（for term 8）挂了，a、d和f，哪个会被选举为leader？谁投的票？新leader产生后，哪些entry一定会保留下来？
&lt;img src=&#34;http://localhost:1313/images/2019/15639591300953.jpg&#34;
	
	
	
	loading=&#34;lazy&#34;
	
		alt=&#34;-w478&#34;
	
	
&gt;&lt;/p&gt;
&lt;p&gt;a-f每个server最多可能获得投票的情况如下：
a：a，b，e，f
b：b，f
c：a，b，c，e，f
d：a，b，c，d，e，f
e：b，e
f：f&lt;/p&gt;
&lt;p&gt;所以a或d可能成为leader，一定会保留下的log是111445566。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;如果server刚开始选举，此时收到了来自leader的AppendEntries RPC（假设leader与这个server都是处于同一个term），此时要怎么处理？&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;candidate成为leader后，应该立即发送心跳来避免发生多余的选举。&lt;/li&gt;
&lt;li&gt;如果发生这个情况，由于term一样，leader会忽略RequestVote RPC。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;如果leader完成复制，更新自身的commit index，且完成了apply，但在通知follower commit index前挂了（或丢失了leader资格），这个entry最终会丢失吗？&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;lastApplied是否应该做持久化？
这个地方有点争议。在&lt;a class=&#34;link&#34; href=&#34;https://github.com/ongardie/dissertation&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;ongardie/dissertation&lt;/a&gt;的Errata中，指明了lastApplied是否做持久化应该与状态机一致。但在&lt;a class=&#34;link&#34; href=&#34;https://thesquareplanet.com/blog/raft-qa/&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;Raft Q&amp;amp;A&lt;/a&gt;中的说法是，状态机如果做持久化，那么也应该负责记住执行了哪些log。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;follower拒绝AppendEntries RPC时，如何快速的解决冲突？
follower拒绝时，返回冲突entry的term，以及这个term首个entry的index。&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;如果leader有这个entry，那么移动nextIndex到冲突的那个entry。&lt;/li&gt;
&lt;li&gt;如果leader没有这个entry，那么移动nextIndex到那个term的首个index。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;还可以使用二分法，查找首个冲突的entry，这可以实现在最坏情况下的最佳时间复杂度。&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;h1 id=&#34;lab-2a-raft&#34;&gt;Lab 2A: Raft
&lt;/h1&gt;&lt;h2 id=&#34;锁的使用建议&#34;&gt;锁的使用建议
&lt;/h2&gt;&lt;p&gt;这个&lt;a class=&#34;link&#34; href=&#34;https://pdos.csail.mit.edu/6.824/labs/raft-locking.txt&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;lecture&lt;/a&gt;说了几点关于锁的建议，我觉得最重要的一点是，&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;Be careful about assumptions across a drop and re-acquire of a
lock. One place this can arise is when avoiding waiting with locks
held.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;在减少锁的粒度的时候，可能也把变量的write和read分开了，进而导致在read的时候，变量已经被其他线程改变了。&lt;/p&gt;
&lt;h1 id=&#34;references&#34;&gt;References
&lt;/h1&gt;&lt;ol&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;http://catkang.github.io/2017/11/30/raft-safty.html&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;Why Raft never commits log entries from previous terms directly&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://www.jianshu.com/p/a89084103b6e&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;raft之 为什么不能直接commit 上一个term的entry&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://yuerblog.cc/2018/07/28/understand-raft/&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;raft本质理解&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://www.youtube.com/channel/UCy1xUHx0OWo3gnBC8AbnUqg&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;Raft user study lectures&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://www.infoq.com/presentations/raft-consensus-algorithm/&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;The Raft Consensus Algorithm&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://github.com/ongardie/dissertation&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;ongardie/dissertation&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://runway.systems/?model=github.com/ongardie/runway-model-raft&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;ongardie/runway-model-raft&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://thesquareplanet.com/blog/raft-qa/&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;Raft Q&amp;amp;A&lt;/a&gt;&lt;/li&gt;
&lt;/ol&gt;
</description>
        </item>
        <item>
        <title>6.824 2018 Lecture 4 Primary-Backup Replication</title>
        <link>http://localhost:1313/posts/2019/2019-07-11-6.824-spring-2018-lecture-4/</link>
        <pubDate>Thu, 11 Jul 2019 17:46:17 +0000</pubDate>
        
        <guid>http://localhost:1313/posts/2019/2019-07-11-6.824-spring-2018-lecture-4/</guid>
        <description>&lt;h1 id=&#34;readings---practical-system-for-fault-tolerant-virtual-machines-fault-tolerant-virtual-machines论文&#34;&gt;Readings - Practical System for Fault-Tolerant Virtual Machines Fault-Tolerant Virtual Machines论文
&lt;/h1&gt;&lt;p&gt;这篇论文讨论的主从复制与常见的相比，非常极端和雄心勃勃，论文基于vm构建了一个os级别的主从复制系统，较为细致的讨论了主从复制的设计和实现。&lt;/p&gt;
&lt;h2 id=&#34;介绍&#34;&gt;介绍
&lt;/h2&gt;&lt;p&gt;主从复制是实现server容错的常用方法，如果primary挂了，backup可以继续提供服务。将primary的全量数据同步到backup是比较耗费带宽的操作。更好的方法是将server视为确定状态机，基本思路是把primary和backup置为相同的初始状态，并保证二者都能以相同的顺序收到相同的输入。由于存在某些操作是非确定的（如：中断和获得当前时间），因此还需要额外的协同。&lt;/p&gt;
&lt;h2 id=&#34;ft协议&#34;&gt;FT协议
&lt;/h2&gt;&lt;h3 id=&#34;deterministic-replay&#34;&gt;Deterministic Replay
&lt;/h3&gt;&lt;p&gt;为了实现在backup进行replay，需要保证：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;正确的捕获所有输入和非确定的操作，以保证backup的确定性执行。&lt;/li&gt;
&lt;li&gt;在backup上正确的replay。&lt;/li&gt;
&lt;li&gt;对性能没有影响。&lt;/li&gt;
&lt;/ol&gt;
&lt;blockquote&gt;
&lt;p&gt;论文中关于非确定的操作，零零散散在不少地方提到，不过总结下来其实就是，这个操作不是pure function，结果受除输入的外部状态影响。更具体的就是，非确定操作使用到的资源是与其他（来自vm或hypervisor）的进程共享的，竞争导致了非确定的结果。&lt;/p&gt;
&lt;p&gt;在多核系统上，上述的竞争会更复杂，这也是为什么论文没有在多核系统上实现的原因。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h3 id=&#34;ft协议-1&#34;&gt;FT协议
&lt;/h3&gt;&lt;p&gt;primary的日志实时的通过logging channel发送到backup。为了保证在切换的时候，backup能够以与primary相同状态进行服务，需要满足：&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Output Requirement&lt;/strong&gt;：如果primary失败，backup接替了primary，backup的执行需要保证所有的输出都与primary的一致。&lt;/p&gt;
&lt;p&gt;只要满足Output Requirement，在发生主从切换的时候，client就感知不到打断和不一致。为了保证Output Requirement，需要backup在收到能够产生那个output的input之前，primary推迟输出，也就是下面的规则：&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Output Rule&lt;/strong&gt;：只有当backup收到能够产生一个output的log，并返回ack（表示已经存入log buffer）给primary，primary才能输出。&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;除了VM-FT有，在其他的多副本系统上都以某种形式出现着。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;&lt;img src=&#34;http://localhost:1313/images/2019/15628165996172.jpg&#34;
	
	
	
	loading=&#34;lazy&#34;
	
		alt=&#34;-w470&#34;
	
	
&gt;&lt;/p&gt;
&lt;p&gt;primary等待ack不必阻塞系统当前的执行。&lt;/p&gt;
&lt;h3 id=&#34;检测和响应失败&#34;&gt;检测和响应失败
&lt;/h3&gt;&lt;p&gt;检测失败：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;server之间的heartbeat&lt;/li&gt;
&lt;li&gt;logging channel的traffic是否一直存在&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;如何避免split-brain：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;共享磁盘存储，原子的检测标志位&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id=&#34;实现&#34;&gt;实现
&lt;/h2&gt;&lt;h3 id=&#34;ft-vm启动&#34;&gt;FT VM启动
&lt;/h3&gt;&lt;ul&gt;
&lt;li&gt;使用基于VMware VMotion实现的FT VMotion完成clone，primary的中断小于1s。&lt;/li&gt;
&lt;li&gt;使用VMware vShpere实现的集群服务来选择最佳server创建backup。&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;logging-channel管理&#34;&gt;logging channel管理
&lt;/h3&gt;&lt;p&gt;&lt;img src=&#34;http://localhost:1313/images/2019/15628298304504.jpg&#34;
	
	
	
	loading=&#34;lazy&#34;
	
		alt=&#34;-w322&#34;
	
	
&gt;&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;log buffer的局限：&lt;/strong&gt;
primary（或backup）的log buffer满了（或空了），primary（或backup）需要等待至log buffer可用为止，此时会暂停对client的服务。&lt;/p&gt;
&lt;p&gt;主从切换的时间=检测primary失败的时间+backup消费log buffer的时间，为了减少切换的时间，在backup无法及时消费log的时候，会减慢primary的速度（限制CPU）。&lt;/p&gt;
&lt;h3 id=&#34;ft-vm上的操作&#34;&gt;FT VM上的操作
&lt;/h3&gt;&lt;p&gt;除VMotion外的所有运维操作（例如：调整cpu限制），都必须在primary上完成，然后通过特殊的日志项目发送到backup。&lt;/p&gt;
&lt;h3 id=&#34;关于磁盘io的实现问题&#34;&gt;关于磁盘io的实现问题
&lt;/h3&gt;&lt;ol&gt;
&lt;li&gt;磁盘访问时的竞争会导致非确定的操作。
&lt;ul&gt;
&lt;li&gt;检测这样的io竞争，并在primary和backup上以相同顺序执行。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;使用DMA时，对磁盘和内存的访问会导致非确定的操作。
&lt;ul&gt;
&lt;li&gt;对磁盘的读写，改为读写bounce buffer。&lt;/li&gt;
&lt;li&gt;只有当数据全部读到buffer以后，FT才会&lt;em&gt;从读指令处&lt;/em&gt;恢复primary/backup的执行，避免二者出现差异（如果直接依赖DMA读取，primary和backup可能会在不可预知的时间点发生缺页中断，从而导致不一致）。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;primary在磁盘io完成前就挂了。
&lt;ul&gt;
&lt;li&gt;backup切换为primary以后，无从得知io是否已经完成。FT会在backup上重试（前两点已经消除了导致非确定结果的操作）未完成的io操作。&lt;/li&gt;
&lt;li&gt;那么&lt;em&gt;如何判断未完成的io操作是哪些&lt;/em&gt;？io完成以后，设备会产生一个中断，如果某个io log缺少相应的中断，那么就需要重试。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;primary在磁盘io完成后挂了
&lt;ul&gt;
&lt;li&gt;backup同样无从得知io是否已经完成。此时重新执行写磁盘是幂等的（而对于网络io，tcp会忽略重复的包）。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;h3 id=&#34;关于网络io的实现问题&#34;&gt;关于网络io的实现问题
&lt;/h3&gt;&lt;p&gt;hypervisor对vm buffer的更新导致了非确定操作，改为由vm触发hypervisor的中断，并记录到log。&lt;/p&gt;
&lt;h2 id=&#34;其他设计&#34;&gt;其他设计
&lt;/h2&gt;&lt;h3 id=&#34;非共享磁盘&#34;&gt;非共享磁盘
&lt;/h3&gt;&lt;p&gt;优势：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;磁盘作为vm的内部状态，primary写磁盘时不必等待backup的ack。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;缺点：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;backup失败、执行FT VMotion时，需要同步磁盘数据。&lt;/li&gt;
&lt;li&gt;需要使用其他方法来解决split-brain，例如：另外一个server作为共享存储；多个机器进行投票选主。&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;backup上执行read&#34;&gt;backup上执行read
&lt;/h3&gt;&lt;p&gt;在从执行read，论文考虑的点并不是可以增加系统的read上限，而是考虑不发送read log可以减少logging channel的带宽。&lt;/p&gt;
&lt;p&gt;具体看在backup上执行read，需要注意的点是：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;如果primary成功，backup失败，backup需要重试，直到成功。&lt;/li&gt;
&lt;li&gt;如果primary失败，那么尝试读取的内存必须发送到backup。&lt;/li&gt;
&lt;li&gt;如果primary执行了读，然后是写，那么在backup也必须按照这个顺序来执行。&lt;/li&gt;
&lt;/ul&gt;
&lt;h1 id=&#34;lecture&#34;&gt;Lecture
&lt;/h1&gt;&lt;h2 id=&#34;fault-tolerance&#34;&gt;Fault tolerance
&lt;/h2&gt;&lt;p&gt;&lt;strong&gt;理想的特性&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;可用性&lt;/li&gt;
&lt;li&gt;强一致性&lt;/li&gt;
&lt;li&gt;对client和server software透明&lt;/li&gt;
&lt;li&gt;高效&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;容错，容什么错？&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Fail-stop failures&lt;/li&gt;
&lt;li&gt;Independent failures&lt;/li&gt;
&lt;li&gt;Network drops some/all packets&lt;/li&gt;
&lt;li&gt;Network partition&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;不包括：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Incorrect execution&lt;/li&gt;
&lt;li&gt;Correlated failures&lt;/li&gt;
&lt;li&gt;Configuration errors&lt;/li&gt;
&lt;li&gt;Malice&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id=&#34;fault-tolerant-mapreduce-master&#34;&gt;Fault tolerant MapReduce master
&lt;/h2&gt;&lt;p&gt;lab 1中的woker由于是无状态的，且mapper和reducer执行的都是pure function，因此实现容错很简单。&lt;/p&gt;
&lt;p&gt;master实现容错，需要考虑：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;需要复制什么状态？应用程序级？指令级？（woker list、完成的job、空闲的worker、tcp连接状态、程序memory和stack、cpu寄存器？）&lt;/li&gt;
&lt;li&gt;primary是否需要等待backup？&lt;/li&gt;
&lt;li&gt;什么时候切换到backup？&lt;/li&gt;
&lt;li&gt;切换时是否能被观察到？&lt;/li&gt;
&lt;li&gt;如何快速的切换？&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;主要方法有&lt;/strong&gt;&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;State transfer，简单，但是state可能会很大，传输很慢
&lt;ul&gt;
&lt;li&gt;主副本执行服务&lt;/li&gt;
&lt;li&gt;primary把新状态发送到backup&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;Replicated state machine，高效但复杂
&lt;ul&gt;
&lt;li&gt;所有副本执行所有的操作&lt;/li&gt;
&lt;li&gt;如果所有副本有相同的起始状态、相同的操作、相同的顺序、deterministic，那么就会有相同的最终状态&lt;/li&gt;
&lt;li&gt;例如：VM-FT，GFS&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;h2 id=&#34;vm-ft&#34;&gt;VM-FT
&lt;/h2&gt;&lt;p&gt;VM-FT是一个replicated state machine。&lt;/p&gt;
&lt;p&gt;为了避免primary和backup出现差异，backup必须在指令流的相同位置、以相同的顺序看到相同事件。对于普通的指令，这个是比较容易实现的。FT对中断和磁盘/网络io会做特殊的处理。&lt;/p&gt;
&lt;p&gt;例如：时钟中断&lt;/p&gt;
&lt;p&gt;&lt;img src=&#34;http://localhost:1313/images/2019/ft%20time%20interrupt.jpg&#34;
	
	
	
	loading=&#34;lazy&#34;
	
		alt=&#34;ft time interrupt&#34;
	
	
&gt;&lt;/p&gt;
&lt;h2 id=&#34;ft的有缺点&#34;&gt;FT的有缺点
&lt;/h2&gt;&lt;p&gt;&lt;strong&gt;适用场景&lt;/strong&gt;&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;重要且低延迟的服务，例如：name server。&lt;/li&gt;
&lt;li&gt;不便于修改的服务。&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;strong&gt;不适用的情况&lt;/strong&gt;&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;高吞吐量的服务
&lt;ul&gt;
&lt;li&gt;state仅仅是应用程序级别的，降低了开销，实现更高的性能。&lt;/li&gt;
&lt;li&gt;例如：GFS适用应用程序级别的state。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;h1 id=&#34;reference&#34;&gt;Reference
&lt;/h1&gt;&lt;ol&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;http://alvaro-videla.com/2013/12/failure-modes-in-distributed-systems.html&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;FAILURE MODES IN DISTRIBUTED SYSTEMS&lt;/a&gt;&lt;/li&gt;
&lt;/ol&gt;
</description>
        </item>
        <item>
        <title>6.824 2018 Lecture 3 GFS</title>
        <link>http://localhost:1313/posts/2019/2019-05-26-6.824-spring-2018-lecture-3/</link>
        <pubDate>Sun, 26 May 2019 08:38:33 +0000</pubDate>
        
        <guid>http://localhost:1313/posts/2019/2019-05-26-6.824-spring-2018-lecture-3/</guid>
        <description>&lt;h1 id=&#34;readings---the-google-file-system论文&#34;&gt;Readings - The Google File System论文
&lt;/h1&gt;&lt;h2 id=&#34;介绍&#34;&gt;介绍
&lt;/h2&gt;&lt;p&gt;GFS是由Google设计和实现的，以满足Google对数据处理快速正常的需求。GFS和先前的分布式文件系统有很多相似的目标，例如：性能、可扩展性、可靠性和可用性。然而，GFS的设计是由Google对应用负载和技术环境的关键（当前和预期的）观察驱动的，这反映了与早期文件系统设计假设的显著不同。Google重新审视传统的选择，并探索在设计领域探索了彻底不同的观点。&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;组件故障是常态而非例外。
因此持续的监控、错误监测、容错和自动恢复是系统不可缺少的。&lt;/li&gt;
&lt;li&gt;传统标准的文件是巨大的，常常很多GB。处理包含数十亿个对象、很多TB、且快速增长的数据集时，即便文件系统可以支持，也很难管理数十亿个约KB大小的文件。
因此设计假设和参数，例如IO操作和block大小必须被重新审视。&lt;/li&gt;
&lt;li&gt;大多数文件修改都是通过追加新数据的方式，而非覆盖已有的数据。对文件的随机写几乎已经不存在了。一旦写入，文件就是只读的，且常常只是顺序读。
鉴于这样对大文件的访问模式，追加成为了性能优化和原子保证的的关注点，而在client上对数据块的缓存失去了吸引力。&lt;/li&gt;
&lt;li&gt;应用程序和文件系统api的协同设计，通过提升灵活性，有益于整个系统。&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id=&#34;设计概览&#34;&gt;设计概览
&lt;/h2&gt;&lt;h3 id=&#34;接口&#34;&gt;接口
&lt;/h3&gt;&lt;p&gt;虽然GFS没有实现例如POSIX的标准API，但也提供了一组熟悉的文件系统接口。文件以层次结构的方式用目录组织起来，并用路径名来标识。GFS支持&lt;em&gt;create，delete，open，close，read&lt;/em&gt;和&lt;em&gt;write&lt;/em&gt;文件。GFS还支持&lt;em&gt;snapshot&lt;/em&gt;和&lt;em&gt;record append&lt;/em&gt;。&lt;/p&gt;
&lt;h3 id=&#34;架构&#34;&gt;架构
&lt;/h3&gt;&lt;p&gt;一个GFS集群由一个&lt;em&gt;master&lt;/em&gt;和多个&lt;em&gt;chunkserver&lt;/em&gt;构成，可以被多个&lt;em&gt;client&lt;/em&gt;访问。&lt;/p&gt;
&lt;p&gt;&lt;img src=&#34;http://localhost:1313/images/2019/15584874415545.jpg&#34;
	
	
	
	loading=&#34;lazy&#34;
	
		alt=&#34;-w757&#34;
	
	
&gt;&lt;/p&gt;
&lt;p&gt;文件被切分为固定大小的&lt;em&gt;chunks&lt;/em&gt;。每个chunk都由一个不可变，且全局唯一的64位&lt;em&gt;chunk handle&lt;/em&gt;标识，这个chunk handle在创建chunk时master分配的。chunkservers把chunk作为linux文件存储在本地磁盘上，并通过指定的chunk handle和byte range来读写文件。为了reliability（可靠性），每个chunk会在多个chunkservers上有复制。&lt;/p&gt;
&lt;p&gt;master维护这整个文件系统的metadata，包括namespace，访问控制信息，文件到chunk的映射，以及chucks的当前位置。master还可控制着系统级别的活动，例如chuck租约管理，孤儿chunk的垃圾回收，以及chunkservers之间的chunk迁移。master定期与每个chunkserver以HeartBeat消息的形式进行通信，完成指令的发送和状态的收集。&lt;/p&gt;
&lt;p&gt;client与master通信来进行metadata相关的操作，数据相关的通信是直接与chunkserver进行的。&lt;/p&gt;
&lt;p&gt;client和chunkserver都不需要缓存数据。client缓存数据的收益很小，因为大多数程序都是流式读取大文件或者数据量太大而无法缓存。client不用缓存数据消除了缓存一致性的问题，简化了系统设计。但client会缓存metadata。chunkserver不必缓存文件数据，因为chunk都是以本地文件的形式保存的，linux buffer cache会把常访问的数据放入内存。&lt;/p&gt;
&lt;h3 id=&#34;单一master&#34;&gt;单一master
&lt;/h3&gt;&lt;p&gt;单一master极大的简化了设计，并使master能够使用全局信息来进行复杂的chunk布局和复制决策。然而当读写时，必须最小化master的参与，这样master才不会成为瓶颈。client进行读写时，先询问master应该连接哪个chunkserver，并缓存这个信息一段时间，然后直接与这个chunkserver交互来完成后续的操作。&lt;/p&gt;
&lt;h3 id=&#34;chunk-size&#34;&gt;chunk size
&lt;/h3&gt;&lt;p&gt;chunk size是关键设计参数之一。GFS使用一个远大于典型文件系统的block size，64MB。每个chunk副本都以普通linux文件的形式存储在chunkserver，并在需要的时候扩展。惰性空间分配避免了由于内部碎片导致的空间浪费。&lt;/p&gt;
&lt;p&gt;大型chunk size有这些优势，&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;减少了client与master交互的需求。
读写同一个chunk只需要向master请求一次chunk的位置信息。&lt;/li&gt;
&lt;li&gt;由于chunk较大，client也较为可能在一个给定的chunk上进行很多的操作。
通过在较长时间内保持与chunkserver的TCP连接，可以减少网络开销。&lt;/li&gt;
&lt;li&gt;减少了存储在master上metadata的大小。
由此可以将metadata放入内存。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;然而大型chunk size，即便有惰性空间分配，也存在弊端，&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;chunkserver的热点访问。
一个小文件仅有为数不多的chunks组成，可能就一个。如果大量的client都访问同一个文件，那么存储这些chunk的chunkserver可能会成为热点。&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;metadata&#34;&gt;Metadata
&lt;/h3&gt;&lt;p&gt;mater主要存储3类metadata：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;文件和chunk namespace&lt;/li&gt;
&lt;li&gt;文件到chunks的映射关系&lt;/li&gt;
&lt;li&gt;每个chunk副本的位置&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;其他metadata还包括所有权和权限、每个chunk的版本、引用计数（用于实现copy-on-write）。&lt;/p&gt;
&lt;p&gt;所有的metadata都是存储在内存中的。&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;前两种还会进行持久化存储（使用write-ahead log），这是通过把记录修改到操作日志、在master落盘、以及在远程机器上存放副本来实现的。&lt;/li&gt;
&lt;li&gt;对于最后一种metadata，master并不会做持久化存储。在master启动和有chunkserver加入集群的时候，master会询问每一个chunkserver存放的chunks。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;In-Memory Data Structures&lt;/strong&gt;
由于metadata是存放在内存中的，因此mater的操作很快，除此之外还能完成定期在后台较快的完整扫描。这个定期扫描用于实现chunk的gc，chunkserver故障时副本重新复制，以及chunk的迁移。&lt;/p&gt;
&lt;p&gt;一个潜在的问题是存储的chunk数目受限于内存的大小。但由于每个chunk的metadata少于64byte，且文件的namespace数据也少于64byte，并且启用了前缀压缩，因此这不是一个严重的问题。如果确实有必要支持更大的文件系统，加内存即可。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Chunk Locations&lt;/strong&gt;
master并不对chunk locations做持久化。master启动时，会向所有chunkserver请求。之后，由于master控制着所有chunks的放置，并通过心跳消息来监控chunkserver，master能够确保自身的信息是最新的。&lt;/p&gt;
&lt;p&gt;为什么不做持久化？&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;消除了master和chunkserver的同步问题。
chunkserver可能加入、离开、重启、重命名、故障等。&lt;/li&gt;
&lt;li&gt;chunkserver对自己有和没有哪些chunk有最终的话语权。
在master维护此信息的一致视图是没有意义的，chunkser可能出现1中的各种问题。&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;strong&gt;Operation Log&lt;/strong&gt;
操作日志对GFS很重要，&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;包含了metadata关键修改的历史记录，并持久化。&lt;/li&gt;
&lt;li&gt;作为逻辑时间戳，定义了并发操作的顺序。
文件和chunks，以及它们的版本，全都在创建的时候被逻辑时间唯一且永久的标识。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;可靠性保证，&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;仅当metadata的修改完成持久化以后，这些修改才对client可见。&lt;/li&gt;
&lt;li&gt;在多个远程机器上有复制。
&lt;ul&gt;
&lt;li&gt;仅当把相应的log记录写入本地和远程机器的磁盘后，才响应client。&lt;/li&gt;
&lt;li&gt;master会批量flush日志，来减少flush和复制对整个集群吞吐量的影响。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;master通过重放操作日志来恢复文件系统的状态。
&lt;ul&gt;
&lt;li&gt;为了减少启动的时间，需要保证log较小。&lt;/li&gt;
&lt;li&gt;当log增长超过特定大小时，master会checkpoint自身的状态，以便可以在恢复时载入最后一个checkpoint并重放在那之后的log。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;checkpoint和恢复，&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;checkpoint类似于压缩后的B树，可以直接map到内存，并用用户namespace的查找，且不需要额外的解析。&lt;/li&gt;
&lt;li&gt;master创建checkpoint时，会切换到新log文件，并在另外的线程中创建checkpoint（包含了checkpoint前的所有修改），避免延误当前的修改。&lt;/li&gt;
&lt;li&gt;恢复只需要最近的一个&lt;em&gt;完整&lt;/em&gt;（需要检测是否完整）checkpoint和后续的log文件。更老的checkpoint和log是可以释放的。&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;一致性模型&#34;&gt;一致性模型
&lt;/h3&gt;&lt;p&gt;GFS有一个宽松的一致性模型，这在支持高度分布式应用的同时，也相对简单和高效。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Guarantees by GFS&lt;/strong&gt;
文件namespace修改（例如：创建文件）是原子的，由master专门执行。namespace锁保证了原子性和正确性；master的操作日志定义了这些操作的全局顺序。&lt;/p&gt;
&lt;p&gt;一个文件区域有两种状态，&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;consistent：无论client从哪个副本读取，都能看到相同的数据。&lt;/li&gt;
&lt;li&gt;defined：在文件数据修改后，如果文件区域是consistent的，并且client能看到所有写入到文件的修改。&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;数据修改包含，&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;写入
&lt;ul&gt;
&lt;li&gt;写入操作会把数据写到应用程序指定的文件offset。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;追加
&lt;ul&gt;
&lt;li&gt;即使在并发修改存在的情况下，追加会把数据在GFS选择的offset处&lt;em&gt;至少一次原子&lt;/em&gt;追加一次。&lt;/li&gt;
&lt;li&gt;这个offset会返回给client，标记了一个defined文件区域的起始位置，这个区域包含了已追加记录。&lt;/li&gt;
&lt;li&gt;GFS可能会插入填充或重复记录项。他们占据的区域被认为是inconsistent，且占用户数据总量的很小一部分。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;table&gt;
  &lt;thead&gt;
      &lt;tr&gt;
          &lt;th&gt;&lt;/th&gt;
          &lt;th&gt;Write&lt;/th&gt;
          &lt;th&gt;Record Append&lt;/th&gt;
      &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
      &lt;tr&gt;
          &lt;td&gt;Serial success&lt;/td&gt;
          &lt;td&gt;defined（同时也是consistent）&lt;/td&gt;
          &lt;td&gt;defined interspersed with inconsistent&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
          &lt;td&gt;Concurrent success&lt;/td&gt;
          &lt;td&gt;consistent but undefined&lt;!-- raw HTML omitted --&gt;1. 所有client都能看到相同的数据。&lt;!-- raw HTML omitted --&gt;2. 但是这些数据并不能反映任何修改所写入的内容。&lt;!-- raw HTML omitted --&gt;3. 这些数据一般包含混合了多个修改的片段。&lt;/td&gt;
          &lt;td&gt;defined interspersed with inconsistent&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
          &lt;td&gt;Failure&lt;/td&gt;
          &lt;td&gt;inconsistent（同时也是undefined）&lt;/td&gt;
          &lt;td&gt;inconsistent&lt;/td&gt;
      &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;如何区分defined和undefined的文件区域？&lt;/p&gt;
&lt;p&gt;&lt;em&gt;在一些列的成功修改后，被修改的文件区域保证是defined&lt;/em&gt;（？），且包含了最后一次修改所写入的数据。GFS通过以下方式来实现这个保证，&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;在所有的副本上以相同的顺序对chunk进行修改。&lt;/li&gt;
&lt;li&gt;用chunk版本号来检测过期的副本（由于chunkserver下线导致缺失修改）。
过期的副本不会参与到修改或返回给client，会尽早的被gc掉。&lt;/li&gt;
&lt;/ol&gt;
&lt;blockquote&gt;
&lt;p&gt;不过这里我存疑，例如：concurrent write，并不能保证defined。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;某些client会cache chunk的位置。由于cache的timeout，读取到过期数据的时间窗是有限的。另外，对于大多数文件都是追加操作，一个过期的副本通常会返回过早结束（a premature end of chunk）的文件块，而非过期的数据。&lt;/p&gt;
&lt;p&gt;在修改完成很长时间后，机器故障也会破坏或摧毁数据。GFS通过master和chunkserver定期的握手来检测失效的chunkserver，并通过校验和来检查数据损坏。当问题出现时，数据会尽快的从有效的副本进行恢复。如果在GFS来不及反应（没有足够的处理时间）的时候，所有的副本都丢失了，那应用能收到错误，而不是看到损坏的数据。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Implications for Applications&lt;/strong&gt;
GFS应用可以通过一些已经用于其他目的的简单技术来适应宽松一致性模型，&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;依赖追加而非覆盖，以及checkpointing
&lt;ul&gt;
&lt;li&gt;例如：writer从头生成一个文件，待所有数据写入完毕以后，原子的把文件进行重命名。也可以定期创建checkpoints记录成功写入了多少。checkpoints也可以包含应用级别的校验和。readers只检查和处理至最后一个checkpoint之间的文件区域，这些文件区域是defined。&lt;/li&gt;
&lt;li&gt;追加远比随机写要高效和有弹性的多。&lt;/li&gt;
&lt;li&gt;checkpoint允许writer以增量的方式重新写入，并避免reader处理那些从应用的角度看仍是不完整的数据，尽管这些数据已被成功写入。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;写入时自我校验和自我识别的记录
&lt;ul&gt;
&lt;li&gt;记录是以至少追加一次的语义来记录每个writer的输出的，因而reader需要处理偶然的填充和重复的情况。&lt;/li&gt;
&lt;li&gt;writer写入的每个记录都包含额外的信息，例如校验和，用于验证。reader用校验和来识别并丢弃填充数据，以及记录片段。&lt;/li&gt;
&lt;li&gt;如果无法容忍偶然出现的重复（例如，这些数据会触发非幂等的操作），那可以通过记录中的唯一标识来进行过滤。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;blockquote&gt;
&lt;p&gt;Checkpointing allows writers to restart incrementally and keeps readers from processing successfully written file data that is still incomplete from the application’s perspective.
我没有完全理解所谓的从应用的角度看仍是不完整的数据，是指的最后一个checkpoint之后的那些数据？&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h2 id=&#34;系统交互&#34;&gt;系统交互
&lt;/h2&gt;&lt;p&gt;GFS的设计可以最大限度的减少master参与到所有的操作。&lt;/p&gt;
&lt;h3 id=&#34;租约和修改顺序&#34;&gt;租约和修改顺序
&lt;/h3&gt;&lt;p&gt;文件的修改发生在chunk的所有副本上，GFS使用租约来维护副本之间一致的修改顺序。master会将一个chunk的租借给其中一个副本，这个部分叫做主副本。主副本选择对chunk所有修改的序列顺序，所有的副本在应用这些修改的时候都会遵循这个顺序。最后，全局的修改顺序，首先由master选择的租借授权顺序定义（先选择某个副本为primary，然后可能又选择了另一个为primary），并在租期内由主副本分配的序列号定义。&lt;/p&gt;
&lt;p&gt;租约机制的好处是最小化master的管理开销。&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;租约起始的timeout是60s，但只要chunk还在被修改，主副本可以无限次的请求，然后（一般情况下）得到timeout的扩展。&lt;/li&gt;
&lt;li&gt;timeout扩展的请求的授权是存放在master和所有chunkservers定期交互的心跳包里面的。&lt;/li&gt;
&lt;li&gt;master可能会在租约过期前撤销（例如master要禁止一个正在rename的文件的修改）。&lt;/li&gt;
&lt;li&gt;即使master丢失了与primary的通信，master也可以在老的租约过期后，将新的租约授权给另一个副本。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;img src=&#34;http://localhost:1313/images/2019/15589671997749.jpg&#34;
	
	
	
	loading=&#34;lazy&#34;
	
		alt=&#34;-w368&#34;
	
	
&gt;&lt;/p&gt;
&lt;p&gt;下面以写入为例说明整个过程，&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;client向master请求持有租约的chunkserver以及其他副本的位置。如果租约未被持有，master会授权一个副本。&lt;/li&gt;
&lt;li&gt;master返回给client primary的标识和其他副本的位置。client cache这些数据，且仅在无法连接到primary或primary不在持有租约的时候才会联系master。&lt;/li&gt;
&lt;li&gt;client将数据推送到所有副本，任何顺序均可。每个chunkserver会将这些数据存储在内部的LRU缓存中，直至数据被使用或过期。&lt;/li&gt;
&lt;li&gt;一旦所有副本确认收到数据以后，client向primary发送写请求。写请求标识了先前推送到所有副本的数据。
&lt;ul&gt;
&lt;li&gt;primary为所有收到的修改（可能来自多个client）分配连续的序列号，序列号提供了必要的序列化（necessary serialization）。&lt;/li&gt;
&lt;li&gt;primary将修改以序列号顺序应用到本地状态。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;primary向所有从副本转发写请求。每个从副本都以primary分配的相同序列号顺序应用修改。&lt;/li&gt;
&lt;li&gt;所有从副本回复primary，表明已完成操作。&lt;/li&gt;
&lt;li&gt;primary回复client。&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;如果上述过程出错，&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;任何副本发生错误，都会汇报给client。&lt;/li&gt;
&lt;li&gt;如果在primary发生错误，序列号将不会被分配和转发。&lt;/li&gt;
&lt;li&gt;client的请求被认为已失败，被修改的文件区域将处于不一致状态。&lt;/li&gt;
&lt;li&gt;client会做重试，先会重试步骤3~7，如果不行，会完全从头重试写入。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;如果存在另一个并发写入到相同位置的client，&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;前一个client写入的内容被后一个写入的覆盖&lt;/li&gt;
&lt;li&gt;所有副本都有相同的数据，但是混合了来自两个client的数据，consistent but undefined。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;如果一个写入的数据很大或跨越了chunk的边界，GFS client会把数据打散成多个写操作。这些写操作遵循了上述流程，但是可能会与其他client的并发写操作交替和被覆盖。因此文件区域将会出现consistent but undefined的情况。&lt;/p&gt;
&lt;h3 id=&#34;数据流&#34;&gt;数据流
&lt;/h3&gt;&lt;p&gt;从控制流解耦数据流是为了高效的使用网络，&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;最大化利用每个机器的网络带宽
&lt;ul&gt;
&lt;li&gt;数据以流水线的方式，沿着精心挑选的chunkservers链进行线性的推送，而非以其他拓扑形式来推送（例如：树）。&lt;/li&gt;
&lt;li&gt;这样每个机器的所有出口带宽都会用书尽可能快的传输数据，而非将带宽拆分到多个接收者。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;避免网络瓶颈和高延迟的链路
&lt;ul&gt;
&lt;li&gt;交换机链路通常有这两个问题。&lt;/li&gt;
&lt;li&gt;每个机器会把数据转发到网络拓扑中最近（通过ip地址来估算）的且未收到数据的机器。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;最小化推送所有数据的延迟
&lt;ul&gt;
&lt;li&gt;通过在TCP连接上以pipelining的方式进行数据传输来实现&lt;/li&gt;
&lt;li&gt;一旦某个chunkserver收到一份数据，就立即开始转发。立即转发并不会降低接收数据的速度。&lt;/li&gt;
&lt;li&gt;pipelining在使用全双工链路交换网络的情况下很有用。&lt;/li&gt;
&lt;li&gt;在没有网络拥塞的情况下，传输一份R个副本B byte的数据，理想的时间开销是&lt;code&gt;B/T + RL&lt;/code&gt;，T是网络吞吐量，L是机器间的传输延迟。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;h3 id=&#34;atomic-record-appends&#34;&gt;Atomic Record Appends
&lt;/h3&gt;&lt;p&gt;GFS提供了原子追加记录的操作，叫做record append。&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;传统的写操作中，client指定data需要写入的offset。并发写入到同一个区域是不可序列化的。&lt;/li&gt;
&lt;li&gt;record append中，client指定data，GFS在自己选择的offset处以原子的方式至少一次追加数据到文件末尾，并将offset返回给client。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;record append是一种修改操作，遵循前面描述的控制流。&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;client向文件最后一个chunk的所有副本推送数据，然后向primary发送请求。&lt;/li&gt;
&lt;li&gt;primary检查追加到当前chunk是否会导致chunk超出大小限制。
&lt;ul&gt;
&lt;li&gt;如果是，那么填充当前chunk，并告知从副本也填充。返回client需要在下一个chunk上重试。&lt;/li&gt;
&lt;li&gt;追加记录被限制在&lt;code&gt;0.25 * maximum chunk size&lt;/code&gt;，以限制碎片在可接受的范围内。&lt;/li&gt;
&lt;li&gt;如果没有超过，那么primary进行追加，并告知从副本也在相同的offset追加，最后返回client操作成功。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;如果在任何副本上追加失败，client会重试。&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;重试追加时，&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;重试的结果是，同一个chunk的副本可能会包含不同的数据，可能包括部分或全部相同的记录。&lt;/li&gt;
&lt;li&gt;GFS并不保证所有副本每个字节都是相同的，只能保证数据会以一个原子单位的形式至少一次写入。&lt;/li&gt;
&lt;li&gt;对于一个成功的写入操作，数据一定写在某个chunk的&lt;em&gt;所有副本的相同offset位置&lt;/em&gt;。在这之后，所有副本都至少与记录的结尾一样长。因此任何后续的记录，即使另一个副本成为primary的情况下，都会被分配到更高的offset，或者另一个chunk。&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;snapshot&#34;&gt;Snapshot
&lt;/h3&gt;&lt;p&gt;GFS使用copy-on-write的方式来实现文件或目录的快照，创建snapshot的速度很快。&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;master收到快照请求时，首先撤销将要做snapshot的所有授权chunk的租约。&lt;/li&gt;
&lt;li&gt;租约撤销或过期后，master在磁盘记录操作日志。接着通过复制文件或目录树的metadata，把日志应用到内存状态。新创建的文件快照与原文件指向相同的chunks。&lt;/li&gt;
&lt;li&gt;client写入某个chunk C时，先通过master找到primary。master发现引用计数不唯1，选择一个新的chunk handle C&amp;rsquo;，并通知相关chunkserver本地复制创建C&amp;rsquo;。&lt;/li&gt;
&lt;li&gt;最后client遵循前面的过程进行写入。&lt;/li&gt;
&lt;/ul&gt;
&lt;blockquote&gt;
&lt;p&gt;对目录进行snapshot的时候，复制的metadata是什么？&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h2 id=&#34;master操作&#34;&gt;master操作
&lt;/h2&gt;&lt;p&gt;master执行，&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;所有namespace的操作。&lt;/li&gt;
&lt;li&gt;管理chunk副本的放置、创建、复制、以及与各种系统级活动协调来保证：chunk是完全备份的、平衡chunkserver的负载、回收未使用的空间。&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;namespace管理和锁&#34;&gt;namespace管理和锁
&lt;/h3&gt;&lt;p&gt;GFS使用&lt;em&gt;完整路径名到metadata&lt;/em&gt;的map来从逻辑上表示namespace。存储的时候使用前缀压缩。&lt;/p&gt;
&lt;p&gt;锁以及加锁方式，&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;namespace树的每个节点（绝对文件名或绝对目录名）都有关联的读写锁，读写锁是惰性分配的。&lt;/li&gt;
&lt;li&gt;master进行每个操作前，都会对路径名&lt;code&gt;/d1/d2/.../dn&lt;/code&gt;的每一级父目录加上读锁，并对&lt;code&gt;/d1/d2/.../dn/leaf&lt;/code&gt;加上读锁或写锁，leaf可能是文件或目录。
这里不需要对父级目录加写锁，因为并没有真正意义上的父级目录，也没有需要避免并发修改的数据。只要能防止被删除、重命名或被snapshot即可。&lt;/li&gt;
&lt;li&gt;锁是以一个一致的全序来获取的，以避免死锁。首先按namespace树的层级排序，然后按同一级的字典序排序。&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;副本放置&#34;&gt;副本放置
&lt;/h3&gt;&lt;p&gt;副本放在分布在多个机器内是不够的，还需要分布在不同的机架，放置策略有两个目的：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;最大化数据可靠性和可用性。&lt;/li&gt;
&lt;li&gt;最大化带宽利用。&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;creationre-replicationrebalancing&#34;&gt;Creation，Re-replication，Rebalancing
&lt;/h3&gt;&lt;p&gt;chunks在三种情况下会进行创建：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;master创建新chunk。
创建时会考虑这些因素，
&lt;ul&gt;
&lt;li&gt;chunkserver磁盘使用率低于平均值。&lt;/li&gt;
&lt;li&gt;限制每个chunkserver最近创建数。结合上一点，如果不限制，那么对于一个空chunkserver，迁移时繁重的写操作，会导致磁盘I/O过高。&lt;/li&gt;
&lt;li&gt;跨多个机架。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;副本数低于目标值（由于机器不可达、副本损坏、目标值增加），master重新创建副本。
&lt;ul&gt;
&lt;li&gt;每个需要被re-replicated的chunk的优先级由多个指标衡量：低于目标值多少？优先为存在的文件（live files）创建而不是最近被删除的。优先创建任何阻塞了client操作的的chunk。&lt;/li&gt;
&lt;li&gt;创建时，chunkserver会从有效的副本直接复制，会考虑1中的各种因素。&lt;/li&gt;
&lt;li&gt;为了避免对client流量的影响，master会限制集群和每个chunkserver的clone数目。chunkserver会限制从源chunkserver的读请求来实现限速。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;平衡磁盘空间和负载。
&lt;ul&gt;
&lt;li&gt;master定期进行rebalance，一般来说选择移除磁盘空闲空间低于平均值的。&lt;/li&gt;
&lt;li&gt;新chunkserver会被逐渐的填满，而非立即将新chunk和写流量都直接打上去。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;h2 id=&#34;垃圾回收&#34;&gt;垃圾回收
&lt;/h2&gt;&lt;p&gt;当文件删除的时候，GFS不会立即回收物理存储。GFS会在定期的gc期间，在文件和chunk级别进行回收。&lt;/p&gt;
&lt;h3 id=&#34;机制&#34;&gt;机制
&lt;/h3&gt;&lt;p&gt;当文件被删除的时候，&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;master立即记录删除日志，并将文件重命名到一个包含删除时间的隐藏名字。&lt;/li&gt;
&lt;li&gt;master定期扫描文件系统的namespace时，删除存在3天以上（可配置）的隐藏文件，这有效的切断了与chunks的连接。&lt;/li&gt;
&lt;li&gt;master定期扫描chunk namespace，识别出孤儿chunk，并清除它们的metadata。&lt;/li&gt;
&lt;li&gt;chunkserver与master的心跳包汇报了有哪些chunks，master告知哪些是已经不在metadata中的，可以自由删除。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;这个删除机制相比立即删除，有很多好处，&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;在机器故障很常见的大规模分布式系统中，这个机制简单可靠。
chunk的创建可能只在部分chunkserver上成功；副本删除消息可能丢失，master必须重发。&lt;/li&gt;
&lt;li&gt;将存储回收合并到了mater常规的后台活动里。
可以批量完成，开销被均摊了。&lt;/li&gt;
&lt;li&gt;仅在master相对空闲的时候完成。&lt;/li&gt;
&lt;li&gt;延迟回收提供了应对意外、不可逆删除的保障。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;最大的缺点是，延迟机制妨碍了用户在存储紧张的时候，对空间使用的调优。&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;GFS通过加快对再次明确删除已删除的文件的回收来解决这一问题。&lt;/li&gt;
&lt;li&gt;GFS还允许对namespace的不同部分设置不同的复制和回收机制。&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id=&#34;过期副本检测stale-replica-detection&#34;&gt;过期副本检测（Stale Replica Detection）
&lt;/h2&gt;&lt;p&gt;副本在chunkserver失败且错过修改的时候会过期。&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;master维护了一个&lt;em&gt;chunk version number&lt;/em&gt;来区分最新和过期的副本。在授权租约前，master会增加chunk version number，并通知所有最新的副本。master和所有副本都会将版本号进行持久化存储。&lt;/li&gt;
&lt;li&gt;失败的chunkserver在重启后，master通过心跳包可得知副本过期。&lt;/li&gt;
&lt;li&gt;反过来，如果chunkserver的版本号高于master记录的，master会假设在授权的时候自己失败了，并把更高的版本号作为最新的。&lt;/li&gt;
&lt;li&gt;过期的副本使用gc进移除。&lt;/li&gt;
&lt;li&gt;mater把primary返回给client的时候，以及通知chunkserver进行clone副本的时候，会带上版本号，二者会检查版本号以保证访问到最新的数据。&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id=&#34;容错和诊断&#34;&gt;容错和诊断
&lt;/h2&gt;&lt;h3 id=&#34;高可用&#34;&gt;高可用
&lt;/h3&gt;&lt;p&gt;GFS通过两个简单却有效的策略来实现系统高可用：快速恢复、副本。&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;快速恢复
&lt;ul&gt;
&lt;li&gt;master和chunkserver可快速启动。&lt;/li&gt;
&lt;li&gt;不区分正常和非正常终止。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;chunk副本
&lt;ul&gt;
&lt;li&gt;除副本外，&lt;a class=&#34;link&#34; href=&#34;https://en.wikipedia.org/wiki/Parity_bit&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;parity&lt;/a&gt;和&lt;a class=&#34;link&#34; href=&#34;https://en.wikipedia.org/wiki/Erasure_code&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;erasure coding&lt;/a&gt;对于只读的场景，也是有用的。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;master副本
&lt;ul&gt;
&lt;li&gt;为了master状态的可靠性，操作日志和checkpoint会被复制到多个机器上。只有&lt;em&gt;flush到本地和所有远程机器&lt;/em&gt;上的修改，才认为是已经提交了的。&lt;/li&gt;
&lt;li&gt;提交与写入数据的顺序是什么，在写入数据完成前还是后？&lt;/li&gt;
&lt;li&gt;如果在log复制到多个机器前，master挂了？如果未复制log无法恢复？&lt;/li&gt;
&lt;li&gt;shadow master
&lt;ul&gt;
&lt;li&gt;提供了文件系统的只读访问，因为log可能落后于master；不可写，会导致脑裂。&lt;/li&gt;
&lt;li&gt;与primary相同的顺序apply操作日志。&lt;/li&gt;
&lt;li&gt;启动时定位chunk副本的位置（启动后就不会很频繁），并定期与chunkserver通信监控它们的状态。&lt;/li&gt;
&lt;li&gt;仅当primary更新副本位置时，需要依赖master。&lt;/li&gt;
&lt;li&gt;可被晋升为master。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;主从切换是如何进行的？&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;数据完整性&#34;&gt;数据完整性
&lt;/h3&gt;&lt;ul&gt;
&lt;li&gt;chunkserver使用checksum来检查数据损坏。
&lt;ul&gt;
&lt;li&gt;直接对比两个chunkserver上的副本是不不现实的。&lt;/li&gt;
&lt;li&gt;两个不同的副本&lt;em&gt;可能是&lt;/em&gt;合法的（详见&lt;a class=&#34;link&#34; href=&#34;#Atomic-Record-Appends&#34; &gt;Atomic Record Appends&lt;/a&gt;）。&lt;/li&gt;
&lt;li&gt;每个chunkserver必须独立维护checksum来维护副本完整性。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;chunk结构：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;细分为64KB block。&lt;/li&gt;
&lt;li&gt;每个block都有32bit checksum。&lt;/li&gt;
&lt;li&gt;保留在内存中，且与log一起持久化存储，和用户数据分离。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;对于读操作：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;在返回给client或chunkserver前，会检查与读取范围重叠的部分。因此损坏的数据不会传播开。&lt;/li&gt;
&lt;li&gt;如果checksum不匹配，返回错误给请求方，并通知master。请求方会读取其他副本，master会从其他副本clnoe数据。新副本到位后，通知chunkserver删除错误的副本。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;checksum对（读和追加）性能的影响较小：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;读&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;绝大多数读只跨越少数几个block，因此只需要对少量数据进行校验checksum。&lt;/li&gt;
&lt;li&gt;GFS client在读取是对尝试对齐checksum block的边界。&lt;/li&gt;
&lt;li&gt;checksum的查找和比较不需要做I/O，checksum的计算可以和I/O同时进行。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;追加&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;只需要增量的更新最后一个block的last partial checksum，并计算后续新block的checksum。&lt;/li&gt;
&lt;li&gt;计算last partial checksum block已经损坏，现在无法检测到它，新的checksum也将与存储的数据不匹配，并且在下次读取时将像往常一样检测到损坏。&lt;/li&gt;
&lt;/ul&gt;
&lt;blockquote&gt;
&lt;p&gt;现在无法检测到它（we fail to detect it now），这具体指的是？&lt;/p&gt;
&lt;/blockquote&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;但对于覆盖现有chunk某个range的写操作，必须先读取和检查被覆盖range的首个和最后一个block（为了避免新的checksums可能隐藏存在于未被覆盖区域内的数据损坏），然后写入，最后计算和存储新的checksum。&lt;/p&gt;
&lt;h3 id=&#34;诊断工具&#34;&gt;诊断工具
&lt;/h3&gt;&lt;p&gt;大规模和细致的诊断日志可以极大帮助问题隔离、debug和性能分析，且只有很小的开销。&lt;/p&gt;
&lt;h2 id=&#34;思考问题&#34;&gt;思考问题
&lt;/h2&gt;&lt;p&gt;以下问题部分是我自己提出，部分来自&lt;a class=&#34;link&#34; href=&#34;https://github.com/feixiao/Distributed-Systems/blob/master/Lec03_GFS/Question.md&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;Distributed-Systems/Lec03_GFS/Question.md&lt;/a&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;为什么存储三个副本？而不是两个或者四个？&lt;/li&gt;
&lt;li&gt;为什么不适用RAID？
重点是整机的容错，而非存储设备的容错。&lt;/li&gt;
&lt;li&gt;chunk的大小为何选择64MB？这个选择主要基于哪些考虑？
&lt;ul&gt;
&lt;li&gt;减少了client与master交互的需求。&lt;/li&gt;
&lt;li&gt;由于chunk较大，client也较为可能在一个给定的chunk上进行很多的操作。&lt;/li&gt;
&lt;li&gt;减少了存储在master上metadata的大小。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;master的checkpoint与application checkpointing的区别是什么？&lt;/li&gt;
&lt;li&gt;论文提到append机制可以用于multiple-producer/single-consumer queues，这个具体是如何实现的？&lt;/li&gt;
&lt;li&gt;GFS主要支持追加（append）、改写（overwrite）操作比较少。为什么这样设计？如何基于一个仅支持追加操作的文件系统构建分布式表格系统Bigtable？&lt;/li&gt;
&lt;li&gt;为什么要将数据流和控制流分开？如果不分开，如何实现追加流程？
&lt;ul&gt;
&lt;li&gt;最大化利用每个机器的网络带宽&lt;/li&gt;
&lt;li&gt;避免网络瓶颈和高延迟的链路&lt;/li&gt;
&lt;li&gt;最小化推送所有数据的延迟&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;GFS有时会出现重复记录或者补零记录（padding），为什么？&lt;/li&gt;
&lt;li&gt;租约（lease）是什么？在GFS起什么作用？它与心跳（heartbeat）有何区别？&lt;/li&gt;
&lt;li&gt;GFS追加操作过程中如果备副本（secondary）出现故障，如何处理？如果主副本（primary）出现故障，如何处理？&lt;/li&gt;
&lt;li&gt;GFS master需要存储哪些信息？master数据结构如何设计？&lt;/li&gt;
&lt;li&gt;假设服务一千万个文件，每个文件1GB，master中存储的元数据大概占用多少内存？&lt;/li&gt;
&lt;li&gt;master如何实现高可用性？&lt;/li&gt;
&lt;li&gt;负载的影响因素有哪些？如何计算一台机器的负载值？&lt;/li&gt;
&lt;li&gt;master新建chunk时如何选择chunkserver？如果新机器上线，负载值特别低，如何避免其他chunkserver同时往这台机器迁移chunk？&lt;/li&gt;
&lt;li&gt;如果某台chunkserver报废，GFS如何处理？&lt;/li&gt;
&lt;li&gt;如果chunkserver下线后过一会重新上线，GFS如何处理？&lt;/li&gt;
&lt;li&gt;如何实现分布式文件系统的快照操作？&lt;/li&gt;
&lt;li&gt;chunkserver数据结构如何设计？&lt;/li&gt;
&lt;li&gt;磁盘可能出现“位翻转”错误，chunkserver如何应对？&lt;/li&gt;
&lt;li&gt;chunkserver重启后可能有一些过期的chunk，master如何能够发现？&lt;/li&gt;
&lt;/ul&gt;
&lt;h1 id=&#34;lectures&#34;&gt;Lectures
&lt;/h1&gt;&lt;h2 id=&#34;什么是一致性&#34;&gt;什么是一致性？
&lt;/h2&gt;&lt;ul&gt;
&lt;li&gt;正确性条件&lt;/li&gt;
&lt;li&gt;重要但是当存在数据副本的时候难以实现，尤其是应用并发访问时&lt;/li&gt;
&lt;li&gt;弱一致性
&lt;code&gt;read()&lt;/code&gt;可能返回过期的数据。&lt;/li&gt;
&lt;li&gt;强一致性
&lt;code&gt;read()&lt;/code&gt;使用返回最近&lt;code&gt;write()&lt;/code&gt;的数据。&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id=&#34;理想的一致性模型&#34;&gt;“理想”的一致性模型
&lt;/h2&gt;&lt;ul&gt;
&lt;li&gt;一个存在副本的FS行为和不存在副本的FS一样。&lt;/li&gt;
&lt;li&gt;读可以观察到最近的写。&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id=&#34;实现理想的一致性模型的挑战&#34;&gt;实现“理想”的一致性模型的挑战
&lt;/h2&gt;&lt;ul&gt;
&lt;li&gt;并发&lt;/li&gt;
&lt;li&gt;机器失败：任何操作都可能失败&lt;/li&gt;
&lt;li&gt;网络分区：每个机器/磁盘不是总能访问到的&lt;/li&gt;
&lt;li&gt;为什么这些挑战难以克服？
&lt;ul&gt;
&lt;li&gt;需要c/s间的通信：可能影响性能&lt;/li&gt;
&lt;li&gt;复杂的协议：难以正确的实现&lt;/li&gt;
&lt;li&gt;不少系统没有提供理想的一致性模型&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id=&#34;gfs是否实现了理想的一致性模型&#34;&gt;GFS是否实现了“理想”的一致性模型
&lt;/h2&gt;&lt;ul&gt;
&lt;li&gt;对于目录：实现了
&lt;ul&gt;
&lt;li&gt;强一致性，只有一个副本&lt;/li&gt;
&lt;li&gt;不是高可用，可扩展性受限&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;对于文件：不总是&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id=&#34;总结&#34;&gt;总结
&lt;/h2&gt;&lt;ul&gt;
&lt;li&gt;GFS优秀的地方：
&lt;ul&gt;
&lt;li&gt;顺序读写性能高&lt;/li&gt;
&lt;li&gt;追加&lt;/li&gt;
&lt;li&gt;吞吐量大&lt;/li&gt;
&lt;li&gt;数据容错&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;GFS不好的地方：
&lt;ul&gt;
&lt;li&gt;master容错&lt;/li&gt;
&lt;li&gt;小文件（master是瓶颈）&lt;/li&gt;
&lt;li&gt;client可能看到过期的数据&lt;/li&gt;
&lt;li&gt;追加可能重复&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;h1 id=&#34;lab&#34;&gt;Lab
&lt;/h1&gt;&lt;ul&gt;
&lt;li&gt;Lecture 3没有关于GFS的实验，不过我找到了&lt;a class=&#34;link&#34; href=&#34;https://bitbucket.org/abcdabcd987/ppca-gfs/src/master/&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;ppca-gfs&lt;/a&gt;，看介绍是上交ACM班一个课程的作业，就用这个来补上GFS的实验吧。&lt;/li&gt;
&lt;li&gt;lab的代码在&lt;a class=&#34;link&#34; href=&#34;https://github.com/chaomai/mit-6.824/tree/master/ppca-gfs&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;github.com/chaomai/mit-6.824&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;h1 id=&#34;references&#34;&gt;References
&lt;/h1&gt;&lt;ol&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;http://queue.acm.org/detail.cfm?id=1594206&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;Case Study GFS: Evolution on Fast-forward&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;http://highscalability.com/blog/2010/9/11/googles-colossus-makes-search-real-time-by-dumping-mapreduce.html&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;Google&amp;rsquo;s Colossus Makes Search Real-Time By Dumping MapReduce&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;大规模分布式存储系统：原理解析与架构实战&lt;/li&gt;
&lt;/ol&gt;
</description>
        </item>
        <item>
        <title>6.824 - Spring 2017 Lecture 2 Infrastructure - RPC and threads</title>
        <link>http://localhost:1313/posts/2017/2017-04-20-6.824-spring-2017-lecture-2/</link>
        <pubDate>Thu, 20 Apr 2017 15:27:54 +0000</pubDate>
        
        <guid>http://localhost:1313/posts/2017/2017-04-20-6.824-spring-2017-lecture-2/</guid>
        <description>&lt;h1 id=&#34;lectures&#34;&gt;Lectures
&lt;/h1&gt;&lt;h2 id=&#34;remote-procedure-call-rpc&#34;&gt;Remote Procedure Call (RPC)
&lt;/h2&gt;&lt;p&gt;分布式系统的关键组件。&lt;/p&gt;
&lt;p&gt;目标：简化C/S通信的开发。&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;隐藏 C/S 通信的大多数细节。&lt;/li&gt;
&lt;li&gt;client 端进行 RPC 调用，就像调用普通的函数一样。&lt;/li&gt;
&lt;li&gt;server 端处理 RPC 请求，就像处理普通的函数一样。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;RPC消息发送图示，&lt;/p&gt;
&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;  Client             Server
    request---&amp;gt;
       &amp;lt;---response
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;软件结构，&lt;/p&gt;
&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;  client app         handlers
    stubs           dispatcher
   RPC lib           RPC lib
     net  ------------ net
&lt;/code&gt;&lt;/pre&gt;&lt;h2 id=&#34;一些细节&#34;&gt;一些细节
&lt;/h2&gt;&lt;ul&gt;
&lt;li&gt;组织数据：把数据封装为数据包。&lt;/li&gt;
&lt;li&gt;Binding：client 如何知道应该向谁发送请求？
&lt;ul&gt;
&lt;li&gt;要么在 client 提供 server 的 host name。&lt;/li&gt;
&lt;li&gt;要么有一个 name service，提供 service names -&amp;gt; best server host 的map。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id=&#34;rpc错误处理&#34;&gt;RPC错误处理
&lt;/h2&gt;&lt;p&gt;可能存在的错误，&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;lost packet&lt;/li&gt;
&lt;li&gt;broken network&lt;/li&gt;
&lt;li&gt;slow server&lt;/li&gt;
&lt;li&gt;crashed server&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;client 观察到的错误可能有，&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;client 无法接收到 server 的 response。&lt;/li&gt;
&lt;li&gt;client 无法得知 server 是否接受到了 request。
&lt;ul&gt;
&lt;li&gt;server根本就没有收到请求。&lt;/li&gt;
&lt;li&gt;server执行调用了，但在发送响应前，server出错了。&lt;/li&gt;
&lt;li&gt;server执行调用了，但是发送响应的时候，网络出问题了。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id=&#34;rpc语义&#34;&gt;RPC语义
&lt;/h2&gt;&lt;h3 id=&#34;at-least-oncebest-effort&#34;&gt;at least once（best effort）
&lt;/h3&gt;&lt;p&gt;client 等待 server 的 response，没有响应 -&amp;gt; 重新发送请求，尝试多次仍然没有 response，返回 error。
重新发送请求可能导致 server 重复执行操作。如果重复执行操作是不可接受的，那相关接口必须是幂等的。&lt;/p&gt;
&lt;h3 id=&#34;at-most-once&#34;&gt;at most once
&lt;/h3&gt;&lt;p&gt;由server 检测重复的请求，返回重复请求先前的 reply，而非重复执行。&lt;/p&gt;
&lt;p&gt;如何检测：client 的每个请求包含一个唯一的 ID，&lt;/p&gt;
&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;server:
if seen[xid]:
  r = old[xid]
else
  r = handler()
  old[xid] = r
  seen[xid] = true
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;at most once的难点，&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;如何保证 ID 唯一？尤其是分布式多个 client 的情况下。
&lt;ul&gt;
&lt;li&gt;大随机数？&lt;/li&gt;
&lt;li&gt;唯一的 client ID + request seq id？&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;server 最终是需要丢弃老的 request 的结果的。何时丢弃是安全的？
&lt;ul&gt;
&lt;li&gt;类似TCP sequence #s 和 acks 的方法。使用唯一的 client ID；每个 client 的 RPC 有一个 seq id；对于每个 RPC，client 包含了 seen all replies &amp;lt;= X。&lt;/li&gt;
&lt;li&gt;只允许 client 有一个未完成的 RPC。对于 seq + 1 的 request，server 可以丢弃所有 &amp;lt;= seq。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;当上一个 request 还在运行的时候，如何处理重复的 request？
&lt;ul&gt;
&lt;li&gt;此时 server 还无法查到 reply，但又不想重复执行。可以为每个正在执行的 RPC 加上 pending 标签，重复的 RPC 进行等待/忽略。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;如果 server 挂了或重启怎么办？
&lt;ul&gt;
&lt;li&gt;如果有 memory 中的数据备份，那么 server 可以在重启后忽略并接受重复的请求。&lt;/li&gt;
&lt;li&gt;数据冗余是否应该写入磁盘？&lt;/li&gt;
&lt;li&gt;备份 server 是否也应该有这些数据的备份？&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;exactly-once&#34;&gt;exactly once
&lt;/h3&gt;&lt;p&gt;at most once + 无限重试 + 容错服务。&lt;/p&gt;
&lt;h2 id=&#34;lab-1-mapreduce&#34;&gt;Lab 1: MapReduce
&lt;/h2&gt;&lt;p&gt;这次的实验仍然继续 Lab 1: MapReduce。完成单机版的 MapReduce 以后，还要实现分布式的 MapReduce（模拟），这里需要 RPC 库。&lt;/p&gt;
&lt;p&gt;Go RPC 都提供了 at most once 的语义，换句话说，并不保证送达。如果 worker 没有响应，但实际上是在执行 task 的，那么 master 会重新调度新的 worker 来执行这个任务。&lt;/p&gt;
&lt;p&gt;Lab 1: MapReduce 给出的代码已经提供了框架，下面分析了原始代码中，master 初始化，以及注册 worker 的过程，&lt;/p&gt;
&lt;p&gt;&lt;img src=&#34;http://localhost:1313/images/2017/mapreduce_registration.png&#34;
	
	
	
	loading=&#34;lazy&#34;
	
	
&gt;&lt;/p&gt;
</description>
        </item>
        <item>
        <title>6.824 - Spring 2017 Lecture 1 Introduction</title>
        <link>http://localhost:1313/posts/2017/2017-04-02-6.824-spring-2017-lecture-1/</link>
        <pubDate>Sun, 02 Apr 2017 15:18:30 +0000</pubDate>
        
        <guid>http://localhost:1313/posts/2017/2017-04-02-6.824-spring-2017-lecture-1/</guid>
        <description>&lt;p&gt;最近看 mit-6.824，每个 lecture 会做一次笔记，每个笔记基本都分为，&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Readings：课前阅读的论文&lt;/li&gt;
&lt;li&gt;Lecture：讲义&lt;/li&gt;
&lt;li&gt;Lab：实验&lt;/li&gt;
&lt;/ul&gt;
&lt;h1 id=&#34;readings&#34;&gt;Readings
&lt;/h1&gt;&lt;h2 id=&#34;mapreduce-simplified-data-processing-on-large-clusters论文&#34;&gt;MapReduce: Simplified Data Processing on Large Clusters论文
&lt;/h2&gt;&lt;h3 id=&#34;实现&#34;&gt;实现
&lt;/h3&gt;&lt;p&gt;map task运行时，&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;定期将生成的kv pair写入本地磁盘，并被parition函数分为R个分区。&lt;/li&gt;
&lt;li&gt;这些在磁盘上的pair的位置会被返回给master，master进而把这些位置发给reduce task。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;reduce task运行时，&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;使用RPC读取map task缓存到磁盘的数据。&lt;/li&gt;
&lt;li&gt;当reduce worker读取到所有数据时，按临时key对数据排序。因为多个intermediate key可能会由同一个Reducer处理，因此需要排序使得相同的key在一起。&lt;/li&gt;
&lt;li&gt;reduce worker遍历已排序的中间数据，将key和中间value传给用户定义的reduce函数。&lt;/li&gt;
&lt;/ul&gt;
&lt;h4 id=&#34;master数据结构&#34;&gt;master数据结构
&lt;/h4&gt;&lt;ul&gt;
&lt;li&gt;对于每个map task和reduce task，master保存了任务状态（idle, in-progress, or completed），以及每个worker机器的身份。&lt;/li&gt;
&lt;li&gt;对于每个已完成的map task，master保存了R个分区的中间数据文件的位置和大小，每当一个map task完成，中间数据文件的位置和大小就被更新。这些信息以增量的形式，发送给有正在运行reduce task任务的机器。&lt;/li&gt;
&lt;/ul&gt;
&lt;h4 id=&#34;容错&#34;&gt;容错
&lt;/h4&gt;&lt;p&gt;由于长时间未响应master的ping，worker故障，&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;正在运行的map task或reduce task重置为idle-&amp;gt;重跑。&lt;/li&gt;
&lt;li&gt;已完成的map task重置为idle-&amp;gt;重跑，因为map task生成的中间数据是本地存储的。&lt;/li&gt;
&lt;li&gt;已完成的reduce task无需重跑，因为输出已经保存到GFS。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;当一个map task先被worker A执行，接着又被B执行（worker A失败），这次重跑会被通知到所有执行reduce task的机器。任何还没有从A读取数据的reduce task，都转而向B读取。&lt;/p&gt;
&lt;p&gt;master故障，&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;定期保存master数据结构的checkpoint。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;semantics in the presence of failures，&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;当用户提供的 map 和 reduce 操作的执行结果是确定的，那么分布式的实现也会产生相同的执行结果，这个结果与整个程序顺序执行产生的结果一致。这依赖于，map task 和 reduce task 输出的原子提交。每个正在执行的任务都会把输出写到临时文件。当某个map task完成时，worker向master发送包含有R个临时文件列表的消息。如果master已经收到了这个任务完成的消息，那么本条就会被忽略。而对于 reduce task，输出到GFS的时候，GFS保证了原子性。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;当 map 和 reduce 的执行结果是不确定的，那么最终结果也是不确定的，每个 reduce 任务的执行结果都不同的不确定的串行执行的结果。&lt;/p&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;h4 id=&#34;局部性&#34;&gt;局部性
&lt;/h4&gt;&lt;p&gt;GFS 将文件分为 64MB 的块，并在不同机器上冗余存储。Master 可以利用文件的位置信息，在包含这个副本的机器上调度 map 任务。如果无法进行上述调度，那么就近调度（例如：同一个交换机下的 worker）。&lt;/p&gt;
&lt;h4 id=&#34;任务粒度&#34;&gt;任务粒度
&lt;/h4&gt;&lt;p&gt;MapReduce 把 map 任务和 reduce 任务分别分为 M 份和 R 份，理想情况下，M 和 R 应该远大于机器数目，这样有利于提高动态负载均衡和加速（当某个 worker 挂了情况下的）错误恢复：这个挂了的 worker 所执行的任务可以被重新调度到其他 worker 上进行。&lt;/p&gt;
&lt;p&gt;实现时，master 需要进行 &lt;code&gt;O(M + R)&lt;/code&gt; 次调度，需要 &lt;code&gt;O(M * R)&lt;/code&gt; 状态跟踪 map 产生的临时文件位置（每个 map 产生 R 个临时文件），每个 map/reduce pair 需要一个 byte。&lt;/p&gt;
&lt;p&gt;通常 R 的数量是由用户指定的，实际应用中对 M 的划分是要保证一个分片的数据量大小大约是 16-64M，R 的期望是一个比较小的数。&lt;/p&gt;
&lt;h4 id=&#34;任务备份&#34;&gt;任务备份
&lt;/h4&gt;&lt;p&gt;一个导致任务执行总时间大大延长的重要原因是存在“拖后腿的任务”，这些执行缓慢的任务可能由多种原因导致。一个通用的解决方案是，当一个 MapReduce 任务将要执行完成时，master 调度一个备份执行。当主任务（原始的）或备份执行的其中之一完成时，这个任务被标记完成。这个方法在消耗少许额外资源的情况下，大大减少了总执行时间。&lt;/p&gt;
&lt;h3 id=&#34;改进&#34;&gt;改进
&lt;/h3&gt;&lt;p&gt;提升效率的方法。&lt;/p&gt;
&lt;h4 id=&#34;partitioning-函数&#34;&gt;Partitioning 函数
&lt;/h4&gt;&lt;p&gt;用户指定 reducer 的数目为 R，map 的中间结果按照 partitioning 函数对临时 key 的计算结果分成了 R 个部分。一般来说，默认的 partitioning 函数是足够的，但也可以自己提供 partitioning 函数来实现特定的 partition 目标。&lt;/p&gt;
&lt;h4 id=&#34;保证顺序&#34;&gt;保证顺序
&lt;/h4&gt;&lt;p&gt;每个 partition 的 kv 是按照临时 key 升序的顺序处理的。这保证了，&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;每个 reduce 产生的结果是有序的。&lt;/li&gt;
&lt;li&gt;利于按 key 的随机访问。&lt;/li&gt;
&lt;li&gt;用户觉得有序的结果方便。&lt;/li&gt;
&lt;/ul&gt;
&lt;h4 id=&#34;combiner-函数&#34;&gt;Combiner 函数
&lt;/h4&gt;&lt;p&gt;当，&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;每个 map 任务会产生大量重复的临时 key，&lt;/li&gt;
&lt;li&gt;用户提供的 reduce 函数是可交换和可结合的，&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;可以在 map 任务结束以后，使用 combiner 对 map 任务的结果进行部分合并，减少网络传输的开销。&lt;/p&gt;
&lt;h4 id=&#34;输入输出类型&#34;&gt;输入输出类型
&lt;/h4&gt;&lt;p&gt;MapReduce 提供了读写多种文件格式的支持，用户也可以通过实现相应接口来自定义读写其他格式文件。&lt;/p&gt;
&lt;h4 id=&#34;副作用&#34;&gt;副作用
&lt;/h4&gt;&lt;p&gt;MapReduce 允许 map 或 reduce 生成辅助的输出文件，其原子性依赖于应用的实现。&lt;/p&gt;
&lt;h4 id=&#34;跳过-bad-records&#34;&gt;跳过 Bad Records
&lt;/h4&gt;&lt;p&gt;某些特定的数据会让含有 bug 的 map 或 reduce 函数崩溃，有时无法修复这些 bug，且忽略这些数据是可接受的，那么可以使用 MapReduce 提供的机制来忽略。&lt;/p&gt;
&lt;p&gt;每个 worker 都会监控 segmentation violations 和 bus errors，如果崩溃，发送记录 -&amp;gt; master，如果 master 发现某条记录失败次数大于 1，那么下次执行时就会跳过这条记录。&lt;/p&gt;
&lt;h4 id=&#34;本地执行&#34;&gt;本地执行
&lt;/h4&gt;&lt;p&gt;分布式环境下，debug 一个 map 或 reduce 函数是困难的。MapReduce 库允许在本地执行任务，便于调试。&lt;/p&gt;
&lt;h4 id=&#34;状态信息&#34;&gt;状态信息
&lt;/h4&gt;&lt;p&gt;master 会运行一个内部的 HTTP 服务器，展示状态信息。&lt;/p&gt;
&lt;h4 id=&#34;计数器&#34;&gt;计数器
&lt;/h4&gt;&lt;p&gt;MapReduce 库提供了一个计数器来统计事件发生的次数。worker 响应 master 的 ping 时，计数器的值就被放在 pong 中，发送到 master。master 聚合计数器的值，并在 map 和 reduce 任务结束后返回给用户。当聚合时，重复执行任务的计数器只会被统计一次。有的计数器值是由 MapReduce 库直接维护的。&lt;/p&gt;
&lt;h1 id=&#34;lectures&#34;&gt;Lectures
&lt;/h1&gt;&lt;h2 id=&#34;distributed-system&#34;&gt;Distributed System
&lt;/h2&gt;&lt;p&gt;分布式系统提供了app使用的基础设施，通过抽象隐藏了实现的细节，这些抽象包括：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;存储（storage）&lt;/li&gt;
&lt;li&gt;通信（communication）&lt;/li&gt;
&lt;li&gt;计算（computation）&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;在讨论分布式系统时，下面几个话题会时常出现：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;实现
RPC，线程，并发控制。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;性能&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;理想情况：可扩展的吞吐量。N倍的服务器数量-&amp;gt;（通过并行的CPU，磁盘，网络实现）N倍的吞吐量。因此为了处理更多的负载，只需要添加更多的机器。&lt;/li&gt;
&lt;li&gt;可扩展性随着N增加而变得困难：
&lt;ul&gt;
&lt;li&gt;负载不均衡（Load im-blance），集群里有慢的机器（stragglers）。&lt;/li&gt;
&lt;li&gt;不可并行的代码：初始化，交互&lt;/li&gt;
&lt;li&gt;共享资源的瓶颈：网络&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;容错&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;大集群，复杂的网络-&amp;gt;总会有出问题的地方&lt;/li&gt;
&lt;li&gt;希望能够隐藏这些错误，使得错误对app不可见
&lt;ul&gt;
&lt;li&gt;可用性（Availability）：在出错的情况下，app能继续使用数据。&lt;/li&gt;
&lt;li&gt;持久性（Durability）：修复错误后，app能够继续正常工作。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;big idea：多个服务器。如果一个server crash，client能继续使用其他的。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;一致性（consistency）&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;通用目的的架构需要有良好定义的行为。
&lt;ul&gt;
&lt;li&gt;例如：&lt;code&gt;get(k)&lt;/code&gt;应该返回最近的&lt;code&gt;put(k, v)&lt;/code&gt;。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;实现良好定义的行为是困难的。
&lt;ul&gt;
&lt;li&gt;难以保证多个服务器一致。&lt;/li&gt;
&lt;li&gt;在含有多个操作的更新中，客户端执行到一半可能crash了。&lt;/li&gt;
&lt;li&gt;服务器在尴尬的时刻崩溃，例如：执行了操作但没有返回结果。&lt;/li&gt;
&lt;li&gt;由于网络问题，服务器看起来好像挂了。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;一致性和性能是冲突的。
&lt;ul&gt;
&lt;li&gt;实现一致性需要通信。&lt;/li&gt;
&lt;li&gt;“强一致性”常常导致系统性能低下。&lt;/li&gt;
&lt;li&gt;高性能通常会对app造成“弱一致性”。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;开发者在这个范围内追求了很多设计点（People have pursued many design points in this spectrum）。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id=&#34;mapreduce&#34;&gt;MapReduce
&lt;/h2&gt;&lt;h3 id=&#34;mapreduce概述&#34;&gt;MapReduce概述
&lt;/h3&gt;&lt;ul&gt;
&lt;li&gt;上下文：对海量数据进行多个小时的运算。&lt;/li&gt;
&lt;li&gt;总目标：普通程序员能够在保证合理的效率的情况下，轻松的将数据处理切分到多个服务器。&lt;/li&gt;
&lt;li&gt;需要定义Map和Reduce函数，非并发的代码，且常常是很简单的。&lt;/li&gt;
&lt;li&gt;MR运行在集群上，处理海量数据，隐藏分布式的细节。&lt;/li&gt;
&lt;/ul&gt;
&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;input is divided into M files
Input1 -&amp;gt; Map -&amp;gt; a,1 b,1 c,1
Input2 -&amp;gt; Map -&amp;gt;     b,1
Input3 -&amp;gt; Map -&amp;gt; a,1     c,1
                  |   |   |
                  |   |   -&amp;gt; Reduce -&amp;gt; c,2
                  |   -----&amp;gt; Reduce -&amp;gt; b,2
                  ---------&amp;gt; Reduce -&amp;gt; a,2
&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;
&lt;li&gt;&lt;code&gt;MR&lt;/code&gt;对每个输入文件调用&lt;code&gt;Map()&lt;/code&gt;，生成&lt;code&gt;&amp;lt;k2, v2&amp;gt;&lt;/code&gt;（中间数据）的集合&lt;/li&gt;
&lt;li&gt;&lt;code&gt;MR&lt;/code&gt;收集每个&lt;code&gt;k2&lt;/code&gt;对应的所有&lt;code&gt;v2&lt;/code&gt;，并输入给Reduce调用&lt;/li&gt;
&lt;li&gt;最后从&lt;code&gt;Reduce（）&lt;/code&gt;输出&lt;code&gt;&amp;lt;k2, v3&amp;gt;&lt;/code&gt;的集合&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;mapreduce隐藏了很多痛苦的细节&#34;&gt;MapReduce隐藏了很多痛苦的细节
&lt;/h3&gt;&lt;ul&gt;
&lt;li&gt;在服务器上启动s/w&lt;/li&gt;
&lt;li&gt;跟踪已结束的任务&lt;/li&gt;
&lt;li&gt;数据移动&lt;/li&gt;
&lt;li&gt;错误恢复&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;mapreduce有很好的可扩展性&#34;&gt;MapReduce有很好的可扩展性
&lt;/h3&gt;&lt;p&gt;N台机器-&amp;gt;N倍吞吐量。假设M和R都&amp;gt;=R（大量的输入文件和map输出的key）。由于&lt;code&gt;Map()&lt;/code&gt;之间无交互，可以并行执行，&lt;code&gt;Reduce()&lt;/code&gt;也是，都能够并行执行，因此可以通过添加机器来增加吞吐量。&lt;/p&gt;
&lt;h3 id=&#34;性能受限的潜在因素&#34;&gt;性能受限的潜在因素
&lt;/h3&gt;&lt;p&gt;网络。在Map-&amp;gt;Reduce的shuffle时，所有数据都需要通过网络传输，因此减少通过网络来移动的数据是关键。&lt;/p&gt;
&lt;h3 id=&#34;更多细节&#34;&gt;更多细节
&lt;/h3&gt;&lt;ul&gt;
&lt;li&gt;mater：为worker分配任务，记录中间输出的位置。&lt;/li&gt;
&lt;li&gt;M Map tasks, R Reduce tasks。&lt;/li&gt;
&lt;li&gt;输入存储在GFS，3份冗余。&lt;/li&gt;
&lt;li&gt;集群所有机器都运行GFS和MR worker。&lt;/li&gt;
&lt;li&gt;input task比workder数量更多。&lt;/li&gt;
&lt;li&gt;mater为每个worker分配Map task，在旧任务结束时分发新任务。&lt;/li&gt;
&lt;li&gt;Map worker在本地磁盘将中间key hash到R个partition&lt;/li&gt;
&lt;li&gt;只有当所有Map结束后，本地的Reduce调用才会开始。&lt;/li&gt;
&lt;li&gt;mater告诉Reducer从Map worker获取中间数据partition（intermediate data partitions）。&lt;/li&gt;
&lt;li&gt;Reduce workers将最终结果写入GFS（一个文件对应一个Reduce task）。&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;设计细节减少低速网络的影响&#34;&gt;设计细节：减少低速网络的影响
&lt;/h3&gt;&lt;ul&gt;
&lt;li&gt;Map从本地磁盘读入GFS上的数据副本（replica），而不是从网络。&lt;/li&gt;
&lt;li&gt;中间数据仅仅在网络中传输一次。Map worker将数据写入本地磁盘，而不是GFS。&lt;/li&gt;
&lt;li&gt;中间数据被partition到包含很多key的文件。大网络中的传输更加高效。&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;如何实现良好的load-balance&#34;&gt;如何实现良好的load balance？
&lt;/h3&gt;&lt;p&gt;load balance对于可扩展性很重要，N-1个server都等待1个server结束是不好的。但是有的任务就是会比其他花费更长的时间。&lt;/p&gt;
&lt;p&gt;解决方案：任务数比worker数更多。
master为已完成当前任务的worker分配新的任务。一般来说，就不会有大任务占据主要的计算时间。那么快的server会比慢的server完成更多的任务，最终同时完成所有任务。&lt;/p&gt;
&lt;h3 id=&#34;如何实现容错&#34;&gt;如何实现容错？
&lt;/h3&gt;&lt;p&gt;如果某个server在执行MR任务期间crash怎么办？
不是重启整个job，MR只重新运行失败的&lt;code&gt;Map()&lt;/code&gt;和&lt;code&gt;Reduce()&lt;/code&gt;。这两个操作必须是纯函数：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;不保留调用之间的状态。&lt;/li&gt;
&lt;li&gt;不读取和写入除了MR输入/输入的文件。&lt;/li&gt;
&lt;li&gt;任务之间没有隐藏的通信。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;因此重新执行会生成相同的结果。纯函数的要求是MR相比于其他并发编程模型的主要局限，当它也是MR简洁的关键。&lt;/p&gt;
&lt;h3 id=&#34;crash-recovery的细节&#34;&gt;crash recovery的细节
&lt;/h3&gt;&lt;ul&gt;
&lt;li&gt;Map worker crashes：
&lt;ul&gt;
&lt;li&gt;master观察到worker再也不响应ping。&lt;/li&gt;
&lt;li&gt;crash的worker的中间Map输出丢失，但这个数据有可能每个Reduce任务都需要。&lt;/li&gt;
&lt;li&gt;master根据GFS上输入数据的其他副本来分配任务，并重新执行。&lt;/li&gt;
&lt;li&gt;某些Reduce worker可能已经读取了crash的worker生成的中间数据。此时就需要依赖于&lt;code&gt;Map()&lt;/code&gt;的纯函数特性和确定性。&lt;/li&gt;
&lt;li&gt;如果Reduce获取到了所有的中间数据，那么master就不需要重新运行Map。然后接下来的一个Reduce会crash，进而导致强制重跑失败的Map。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;Reduce worker crashes：
&lt;ul&gt;
&lt;li&gt;已结束的任务不受影响，数据以冗余的形式存储在GFS。&lt;/li&gt;
&lt;li&gt;master重新执行其他worker上未完成的任务。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;Reduce worker在写入输出数据期间crash：
&lt;ul&gt;
&lt;li&gt;GFS的rename是atomic的，在写入完成前数据不可见。因此master可以安全的在其他地方重跑Reduce任务。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;其他错误和问题&#34;&gt;其他错误和问题
&lt;/h3&gt;&lt;ul&gt;
&lt;li&gt;master给两个worker分配了相同的&lt;code&gt;Map()&lt;/code&gt;任务怎么办？
原因可能是master错误的认为worker挂了。master只会将其中一个告诉给Reduce worker。&lt;/li&gt;
&lt;li&gt;master给两个worker分配了相同的&lt;code&gt;Reduce()&lt;/code&gt;任务怎么办？
两个都会尝试在GFS中写入相同的文件。GFS rename的atomic性质避免了结果是两者的混合，只有一个完整的文件可见。而&lt;code&gt;Reduce()&lt;/code&gt;的纯函数特性使得两次输出的文件是一样的。&lt;/li&gt;
&lt;li&gt;如果某个worker很慢怎么办？
原因可能是硬件问题。master重新运行最后几个任务。&lt;/li&gt;
&lt;li&gt;如果worker由于h/w或s/w问题计算出了错误的输出怎么办？
╮(╯_╰)╭，MR假设CPU和software是“&lt;a class=&#34;link&#34; href=&#34;https://github.com/chaomai/mit-6.824/blob/master/docs/LEC%201/FailStop.pdf&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;fail-stop&lt;/a&gt;”的。&lt;/li&gt;
&lt;li&gt;如果master crash了怎么办？
&lt;ul&gt;
&lt;li&gt;从check-point恢复。&lt;/li&gt;
&lt;li&gt;放弃执行任务。&lt;/li&gt;
&lt;li&gt;使用多个master，一个可用，剩余standby。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;什么样的app不适用于mapreduce&#34;&gt;什么样的app不适用于MapReduce？
&lt;/h3&gt;&lt;ul&gt;
&lt;li&gt;不是所有app都适用于map/shuffle/reduce模式。&lt;/li&gt;
&lt;li&gt;小数据量，因为开销很高。例如：网站的后端。&lt;/li&gt;
&lt;li&gt;对海量数据的小更新。例如：为一个大索引添加小文件。&lt;/li&gt;
&lt;li&gt;随机读写，因为Map和Reduce都不能选择输入数据。&lt;/li&gt;
&lt;li&gt;多次shuffle，例如：page-rank。可以使用多个MR来实现，但是不高效。&lt;/li&gt;
&lt;li&gt;更灵活的系统可以实现上述目标，但会导致更复杂的模型。&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;结论&#34;&gt;结论
&lt;/h3&gt;&lt;p&gt;MapReduce让集群计算受欢迎。&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;不是最高效和灵活的。&lt;/li&gt;
&lt;li&gt;可扩展性良好。&lt;/li&gt;
&lt;li&gt;易于编程，错误和数据移动被隐藏了。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;这些在实践中是很好的权衡。&lt;/p&gt;
&lt;h2 id=&#34;lab-1-mapreduce&#34;&gt;Lab 1: MapReduce
&lt;/h2&gt;&lt;ul&gt;
&lt;li&gt;实验给出了框架代码，需要完成关键函数。&lt;/li&gt;
&lt;li&gt;MapReduce的分布式实现除了需要关注task，还需要考虑存储，分布式存储不是这里的重点，因此实验在一台机器上运行worker thread，使用系统的文件系统模拟分布式存储。&lt;/li&gt;
&lt;li&gt;实验要求实现两种模式的MR，顺序执行所有task，以及分别并行执行map和reduce task。实验给出了的框架代码使用了go的channel来实现并行执行task。而对于顺序执行task的实现，实际上是分别把所有map和reduce task做了一次封装，得到“一个map task”和“一个reduce task”，分别执行封装好的“map task”和“reduce task”，其中每个实际的map和reduce都是顺序执行的。&lt;/li&gt;
&lt;li&gt;lab的代码在&lt;a class=&#34;link&#34; href=&#34;https://github.com/chaomai/mit-6.824/tree/master/src/mapreduce&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;github.com/chaomai/mit-6.824&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
</description>
        </item>
        
    </channel>
</rss>
